# rIDM — Working Plan

A modern, multi-tenant Identity Management (IDM) server: OpenID Connect provider,
JWT issuer, user/group/role management, MFA, and identity brokering, with a bundled
admin UI and end-user account console.

Stack: Rust (`api/`) + Next.js static export (`ui/`). Postgres + Valkey.

Open source (MIT). Cloud-agnostic: runs anywhere a container, Postgres, and Valkey run
(bare metal, docker-compose, Kubernetes, any cloud). No provider-specific dependencies.

Rules for executing this plan (from global CLAUDE.md): one sub-phase at a time,
mark `[x]` when done, `cargo check` + commit after each sub-phase, keep README current,
no version bumps outside a release.

---

## 1. Architecture decisions

| Area | Decision | Notes |
|------|----------|-------|
| HTTP framework | **axum 0.8** + tower-http | tower middleware composes cleanly for per-tenant extractors, rate limiting, tracing. Alternative: actix-web for parity with tv/api. Decide before Phase 1. |
| DB | Postgres via **sqlx 0.9** (runtime-tokio, tls-rustls-ring) | Migrations via `sqlx migrate`. No `SELECT *`. |
| Cache / sessions | **Valkey** (Redis protocol via deadpool-redis; Redis 7.2+/8 also works) | Cache-first for tenants, clients, keys, sessions, rate limits, auth-flow state. Invalidate on every write. |
| Tenancy model | **Shared DB, `tenant_id` on every tenant-scoped table** | Composite indexes lead with `tenant_id`. Postgres RLS enabled as defence-in-depth (`SET LOCAL app.tenant_id`). |
| Tenant resolution | **Path prefix**: issuer = `https://{host}/t/{tenant_slug}` | Discovery at `/t/{slug}/.well-known/openid-configuration`. Custom domain per tenant in Phase 9 (host → tenant lookup, cached). |
| Master tenant | Tenant `master` hosts global admins | Global admin roles live here; per-tenant admins live in their tenant. |
| Signing | **Per-tenant key sets**, RS256 default, ES256 + EdDSA supported | Keys stored encrypted at rest (chacha20poly1305, master key from env or mounted file). Rotation with overlap; JWKS publishes active + retiring keys. |
| Access token | **JWT**, short-lived (default 5 min), `iss`/`sub`/`aud`/`azp`/`scope`/`tid`/`roles`/`groups` + custom claim mappers | Optional opaque access tokens per client (introspection-only). |
| Refresh token | **Opaque**, rotated on use, stored hashed (SHA-256), reuse detection revokes family | Redis + Postgres. |
| ID token | Per OIDC Core; `at_hash`, `nonce`, `auth_time`, `amr`, `acr` | |
| Grants | authorization_code + PKCE (S256 required for public clients), refresh_token, client_credentials, device_code (Phase 8), token-exchange (RFC 8693, Phase 9) | **No implicit, no hybrid, no ROPC.** |
| Client auth | client_secret_basic, client_secret_post, private_key_jwt, none (public + PKCE) | |
| Password hashing | argon2id | Tunable params per deployment. |
| Browser SSO session | Server-side in Redis, `__Host-ridm_session_{slug}` cookie (one per tenant; `Path=/`, no `Domain`), HttpOnly, Secure, SameSite=Lax; plain-http dev (`COOKIE_SECURE=false`) drops the `__Host-` prefix and `Secure` | One SSO session per tenant per browser. |
| UI ↔ API auth | **UI is itself an OIDC public client (PKCE) of rIDM** | Admin UI = client of `master`; account console = client of each tenant. Dog-fooding the provider. |
| Login pages + static export | **Flow-handoff pattern**: `/authorize` validates, persists a `login_flow` (Redis), redirects browser to static UI `/login/?flow={id}`; UI calls API to complete steps; API returns final redirect URL to the client's `redirect_uri` | Same pattern for consent, MFA, recovery, verification. No dynamic route segments needed. |
| API docs | utoipa + swagger-ui at `/docs` (disabled in prod by config) | |
| Observability | tracing + tracing-subscriber (JSON), `/healthz`, `/readyz`, Prometheus `/metrics` | |
| Errors | `thiserror` app error → RFC 6749/OIDC error JSON for OAuth endpoints, RFC 9457 problem+json for admin API | |
| Config | env vars via `dotenvy` + typed `Config` struct | 12-factor; same image everywhere. |
| Client IP | `X-Forwarded-For` / `Forwarded` honored only when `TRUSTED_PROXIES` matches the peer | Works behind nginx, Caddy, Traefik, any cloud LB, or none. |
| UI hosting | **Two modes**: (a) API embeds the built `ui/out` via `rust-embed` and serves it (single-binary deploy); (b) `ui/out` on any static host/CDN with `NEXT_PUBLIC_API_URL` set at build | Mode (a) is the default for self-hosters. |
| TLS | Terminated by the operator's reverse proxy, or natively via rustls when `TLS_CERT`/`TLS_KEY` are set | |
| Packaging | Multi-arch container image (amd64/arm64) on GHCR, plus release binaries | Helm chart + docker-compose in `deploy/`. |
| Config as code | Every tenant is exportable/importable as a single JSON document, applied idempotently | GitOps-friendly; the CLI and admin API both support it. |
| Extensibility | Trait-based providers: `KeyEncryptor` (env/file key; KMS/HSM later), `EmailSender` (SMTP/HTTP), `SmsSender` (HTTP webhook), `Captcha` (Turnstile/hCaptcha), `PasswordHasher` (argon2id + legacy verify) | Keeps the core cloud-neutral while allowing optional backends. |
| Events | Internal typed event bus → audit, webhooks, user notifications, cache invalidation (Redis pub/sub across nodes) | Single emit point per domain action. |
| Background jobs | In-process scheduler with Redis-held leader lock | Cleanup, retention, key rotation, email retry, session expiry. |
| Localization | `ui_locales` honored; per-tenant default locale; translation bundles for UI and emails, overridable per tenant | English ships; community translations welcome. |
| Multi-node | Stateless API; all shared state in Postgres/Redis; Redis Sentinel and Cluster supported | Scale horizontally behind any load balancer. |

## 2. Stack and versions (verified 2026-09-14)

All versions are the latest **stable** release as of the date above. Verified by a real
`cargo check` of every backend crate together, and a real `npm install` + `eslint` +
`next build` (static export) of every frontend package together. No alpha/beta/rc/dev builds.

### Toolchain and infrastructure

| Component | Version | Notes |
|-----------|---------|-------|
| Rust | 1.98.1, edition 2024 | |
| Node.js | 24.21.0 (LTS) | 26.x is current but not LTS |
| npm | 12.0.2 | |
| PostgreSQL | 18.6 | minimum supported: 16 |
| Valkey | 9.1.2 | image `valkey/valkey:9.1.2-alpine3.24`; Redis 8.x is protocol-compatible |
| Helm | 4.3.0 | |

### Backend crates (`api/Cargo.toml`)

| Crate | Version | Features / notes |
|-------|---------|------------------|
| axum | 0.8.9 | `macros` |
| tower | 0.5.3 | |
| tower-http | 0.7.1 | `cors`, `trace`, `compression-gzip`, `set-header`, `fs` |
| tower_governor | 0.8.0 | rate limiting |
| tokio | 1.53.1 | `full` |
| sqlx | 0.9.0 | `runtime-tokio`, `tls-rustls-ring`, `postgres`, `uuid`, `chrono`, `json`, `migrate` |
| deadpool-redis | 0.23.1 | `rt_tokio_1` |
| redis | 1.7.0 | `tokio-rustls-comp`, `script` |
| jsonwebtoken | 11.0.0 | `aws_lc_rs` — constant-time RSA; avoids RUSTSEC-2023-0071 exposure |
| rsa | 0.9.10 | `pem` — 0.10 is still RC, do not use |
| p256 | 0.14.0 | `ecdsa`, `pem` |
| ed25519-dalek | 3.0.0 | `rand_core`, `pkcs8`, `alloc` |
| argon2 | 0.6.0 | |
| bcrypt / pbkdf2 / md-5 | 0.19.3 / 0.13.0 / 0.11.0 | legacy hash verification only (imported users) |
| regex | 1.13.1 | profile schema patterns |
| clap | 4.6.7 | `derive`, `env` — the `ridm` CLI (`crates/ridm-cli`) |
| rpassword | 7.5.4 | interactive `ridm-api bootstrap` prompt, `ridm login` and `ridm user` password entry |
| rand_core (0.6, aliased `rand_core_06`) | 0.6.4 | `getrandom` — rsa 0.9 keygen still takes a rand_core 0.6 RNG |
| totp-rs | 6.0.0 | `qr`, `gen_secret` |
| webauthn-rs | 0.5.5 | `danger-allow-state-serialisation` — 0.6 is still dev, do not use. Requires OpenSSL: `openssl-sys 0.9.117` with `vendored` so the distroless image is self-contained |
| chacha20poly1305 | 0.11.0 | |
| sha2 | 0.11.0 | |
| rand | 0.10.2 | |
| rustls | 0.23.45 | `aws-lc-rs` — 0.24 is still dev, do not use |
| reqwest | 0.13.5 | default-features off; `json`, `rustls`, `form` |
| lettre | 0.11.23 | default-features off; `tokio1`, `tokio1-rustls`, `aws-lc-rs`, `rustls-platform-verifier`, `smtp-transport`, `builder` |
| handlebars | 6.4.4 | |
| serde | 1.0.229 | `derive` |
| serde_json | 1.0.151 | |
| uuid | 1.26.1 | `v4`, `v7`, `serde` |
| chrono | 0.4.45 | `serde` |
| thiserror | 2.0.20 | |
| validator | 0.21.0 | `derive` |
| dotenvy | 0.15.7 | |
| tracing | 0.1.44 | |
| tracing-subscriber | 0.3.23 | `env-filter`, `json` |
| metrics-exporter-prometheus | 0.18.3 | |
| utoipa | 5.5.0 | `axum_extras`, `uuid`, `chrono` |
| utoipa-axum | 0.2.0 | |
| utoipa-swagger-ui | 9.0.2 | `axum` |
| rust-embed | 8.12.0 | embedded UI mode |
| testcontainers-modules (dev) | 0.15.0 | `postgres`, `redis` |
| proptest (dev) | 1.11.0 | property tests for token/blob/cursor parsing |
| testcontainers (dev) | 0.27.3 | `reusable-containers` — no Ryuk in testcontainers-rs; named reusable containers instead |
| cargo-llvm-cov / cargo-fuzz / cargo-audit / cargo-deny (tools) | latest stable at Phase 0.9 (verify then) | coverage, fuzzing, supply chain |

### Frontend packages (`ui/package.json`)

| Package | Version | Notes |
|---------|---------|-------|
| next | 16.3.5 | `output: 'export'`, `trailingSlash: true` |
| react / react-dom | 19.3.0 | |
| typescript | **6.0.3** | TS 7.0.2 builds fine but `typescript-eslint` refuses TS 7; revisit when typescript-eslint adds support (tracked upstream in typescript-eslint#10940) |
| eslint | **9.39.5** | ESLint 10.10.0 crashes `eslint-config-next` (bundled eslint-plugin-react 7.37 uses removed `getFilename` API); revisit when eslint-config-next ships a fixed plugin |
| eslint-config-next | 16.3.5 | |
| tailwindcss / @tailwindcss/postcss | 4.3.3 | |
| @radix-ui/react-dialog | 1.1.23 | plus select 2.3.7, and other primitives at their current 1.x/2.x |
| lucide-react | 1.46.0 | |
| @tanstack/react-query | 5.102.8 | |
| @tanstack/react-table | 9.2.4 | |
| ~~@tanstack/react-virtual~~ | — | dropped in 6.4: the React Compiler lint (`react-hooks/incompatible-library`) flags `useVirtualizer` regardless of opt-out directives; the users table windows rows with a small hook of its own (`ui/src/lib/console/virtual.ts`) |
| react-hook-form | 7.88.0 | |
| @hookform/resolvers | 5.9.1 | |
| zod | 4.6.5 | |
| @simplewebauthn/browser | 14.0.0 | |
| recharts | 3.10.1 | |
| qrcode | 1.5.4 | |
| @types/node / @types/react / @types/react-dom / @types/qrcode | 26.1.0 / 19.2.17 / 19.2.3 / 1.5.6 | |
| @playwright/test / @axe-core/playwright | 1.63.0 / 4.13.0 | e2e + accessibility (pinned at 4.12) |

Pin exact versions in both manifests (no `^`) so the lockfiles and the tables above stay
truthful; bump deliberately. The one exception is `crates/ridm-auth`, which is published to
crates.io and lands in other people's dependency graphs: it asks for compatible ranges, since
exact pins in a library conflict with whatever else its dependents already have. The workspace
lockfile still pins what CI builds.

## 3. Repository layout

```
rIDM/
├── Cargo.toml              # workspace (resolver 3, edition 2024, shared deps)
├── crates/
│   ├── ridm-core/          # shared types, provider traits, event definitions
│   ├── ridm-auth/          # published: axum JWT validation for RPs (Phase 10)
│   └── ridm-cli/           # `ridm` command-line tool (Phase 10)
├── api/                    # ridm-api crate
│   ├── migrations/
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── config.rs
│   │   ├── error.rs
│   │   ├── db/             # pool, RLS helper, tx helpers
│   │   ├── cache/          # redis pool + typed cache helpers
│   │   ├── models/         # one file per entity (mod.rs = declarations only)
│   │   ├── repos/          # sqlx queries, tenant-scoped
│   │   ├── services/       # domain logic (tokens, keys, flows, mfa, ...)
│   │   ├── oidc/           # discovery, authorize, token, userinfo, jwks, introspect, revoke, end_session
│   │   ├── flows/          # login/consent/mfa/recovery flow state machine
│   │   ├── routes/         # axum routers (oidc, flows, admin, account, health)
│   │   ├── middleware/     # tenant resolver, auth (bearer/session), rate limit, security headers, CORS
│   │   └── util/
│   ├── tests/              # integration, isolation, security, contract, migration tests
│   ├── fuzz/               # cargo-fuzz targets
│   └── Dockerfile
├── ui/                     # Next.js 16, output: 'export', trailingSlash: true
│   └── src/
│       ├── app/
│       │   ├── login/ register/ invite/ consent/ mfa/ recover/ verify/ logout/ device/ error/   # end-user flows
│       │   ├── account/    # self-service console
│       │   └── admin/      # admin console
│       ├── api-client/     # generated TypeScript client from OpenAPI
│       └── i18n/           # translation bundles
│   └── e2e/                # Playwright tests
├── examples/               # RP samples: nextjs-spa, axum-api, confidential-client
├── perf/                   # k6 load-test scripts + baseline results
├── docs/                   # docs site source
├── deploy/
│   ├── docker-compose.yml  # full stack: api + postgres + redis (dev and small prod)
│   ├── helm/ridm/          # Kubernetes chart
│   └── examples/           # nginx, Caddy, Traefik reverse-proxy snippets
├── .github/workflows/      # ci (all required checks), nightly (full conformance), weekly (long fuzz), release
├── README.md
└── working-plan.md
```

## 4. Data model (tenant-scoped unless noted)

Core identity
- `tenants` (global): id, slug, display_name, status, settings jsonb (password policy, session policy, MFA policy, registration policy, locale, branding, custom_domain, captcha, ip rules), created/updated.
- `signing_keys`: id, tenant_id, kid, alg, public_jwk jsonb, private_key_enc bytea, key_version (master-key generation), status (active|retiring|revoked), not_before, expires_at.
- `users`: id, tenant_id, org_id nullable (reserved for Organizations), username, email, email_verified, phone, phone_verified, password_hash, password_algo (argon2id|bcrypt|pbkdf2|sha256-legacy…), must_change_password, password_expires_at, status (active|disabled|locked|pending|deleted), attributes jsonb, locale, last_login_at, password_changed_at, failed_attempts, locked_until, deleted_at. Unique (tenant_id, username), (tenant_id, email).
- `user_profile_schema`: tenant_id, attributes jsonb (name, type, validation, required, editable_by user|admin, visible_in id_token|userinfo).
- `password_history`: tenant_id, user_id, hash, created_at.
- `credentials`: id, tenant_id, user_id, type (password|totp|webauthn|recovery_code|email_otp|sms_otp), data enc, label, created_at, last_used_at.
- `trusted_devices`: id, tenant_id, user_id, device_hash, name, ua, ip, last_seen_at, expires_at.
- `pending_contact_changes`: tenant_id, user_id, new_email/new_phone, token_hash, expires_at.
- `invitations`: id, tenant_id, email, roles[], groups[], org_id, token_hash, invited_by, expires_at, accepted_at.
- `groups` (nestable via parent_id, attributes jsonb), `group_members`.
- `roles`: id, tenant_id, client_id nullable, name, description; `role_assignments` (user|group → role, org_id nullable); `role_composites`.
- `resource_servers`: id, tenant_id, identifier (audience), name, token_ttl, signing_alg, allow_offline_access; `permissions`: resource_server_id, name, description; `permission_assignments` (role → permission).
- `personal_access_tokens`: id, tenant_id, user_id, name, token_hash, scopes[], expires_at, last_used_at, revoked_at.
- `identity_providers`: id, tenant_id, alias, type (oidc|oauth2|saml|ldap), config enc, mapper config, auto_link policy, status. `federated_identities`: user_id ↔ (idp_id, external_subject).

Clients and protocol
- `clients`: id, tenant_id, client_id, name, client_type (spa|web|native|machine|device), name, logo, description, tos_uri, policy_uri, secret_hashes[] (two active for rotation, each with created_at/expires_at), jwks/jwks_uri, token_endpoint_auth_method, redirect_uris[], post_logout_redirect_uris[], allowed_grants[], allowed_scopes[], allowed_audiences[], token TTL overrides, access_token_format (jwt|opaque), id_token_encryption (alg/enc, none), subject_type (public|pairwise), sector_identifier_uri, require_pkce, require_consent (first-party skip), cors_origins[], initiate_login_uri, backchannel_logout_uri, frontchannel_logout_uri, service_account_user_id nullable, registration_access_token_hash (DCR), status.
- `scopes`: id, tenant_id, name, description (shown on consent), claims[], resource_server_id nullable.
- `claim_mappers`: id, tenant_id, client_id nullable, type (user_attr|group|role|permission|hardcoded|template), config jsonb, include_in (access|id|userinfo).
- `sso_sessions`: id, tenant_id, user_id, auth_time, amr[], acr, ip, user_agent, device_id, expires_at, idle_expires_at, revoked_at (Redis primary, Postgres mirror for listing/revocation).
- `refresh_tokens`: id, tenant_id, family_id, client_id, user_id, session_id, token_hash, scopes[], audiences[], expires_at, consumed_at, revoked_at.
- `authorization_codes`, `login_flows`, `par_requests`, `device_codes` (pending), `jti_denylist` — Redis with TTL.
- `device_codes` (approved/consumed, Postgres for audit).
- `consents`: tenant_id, user_id, client_id, scopes[], granted_at, revoked_at.

Operations
- `audit_events`: id, tenant_id, actor (user|client|admin|system), type, target, ip, ua, details jsonb, prev_hash, hash, created_at. Partitioned by month; hash chain for tamper evidence.
- `login_attempts`: tenant_id, identifier, ip, success, reason, created_at.
- `email_templates`, `sms_templates` (per tenant, per locale, per event; fallbacks to defaults), `smtp_settings`, `sms_settings`, `captcha_settings` (encrypted).
- `outbound_messages`: queued email/SMS with attempts, next_attempt_at, last_error.
- `webhooks`: id, tenant_id, url, secret_hash, events[], status; `webhook_deliveries`: attempts, response_code, next_attempt_at.
- `ip_rules`: tenant_id, client_id nullable, cidr, action (allow|deny), reason.
- `admin_roles` are ordinary roles in `master`/tenant with `ridm:*` permissions; `feature_flags`: tenant_id, key, enabled.
- `organizations` (post-v1, reserved): id, tenant_id, slug, name, domains[], settings; `organization_members`; `organization_invitations`.
- `risk_signals` (post-v1): user_id, session_id, signal type, score, created_at.

## 5. Phases

### Phase 0 — Scaffold and open-source hygiene
- [x] 0.1 Cargo workspace (`resolver = "3"`, edition 2024) with `api/` crate; exact-pinned deps from §2. `cargo check` clean.
- [x] 0.2 `config.rs` (typed env: DATABASE_URL, REDIS_URL, PUBLIC_URL, MASTER_KEY or MASTER_KEY_FILE, BIND_ADDR, LOG_FORMAT, DOCS_ENABLED, COOKIE_SECURE, TRUSTED_PROXIES, TLS_CERT/TLS_KEY), `error.rs`, health endpoints, tracing setup.
- [x] 0.3 `deploy/docker-compose.yml` (api, postgres, redis, mailpit) with dev profile that seeds a tenant, admin, and sample client; `api/Dockerfile` (multi-stage, distroless, multi-arch); `.env.example`.
- [x] 0.4 Migration 0001: `tenants`; seed `master`. `sqlx migrate run` tested; optional migrate-on-start flag.
- [x] 0.5 `ui/` Next 16 static export scaffold (exact-pinned deps from §2). ESLint clean, `next build` produces `out/`.
- [x] 0.6 GitHub Actions CI: `cargo check`, `clippy -D warnings`, `cargo test`, `cargo audit`, `cargo deny`, `npm run lint`, `npm run build`. Renovate config.
- [x] 0.7 README, CONTRIBUTING.md, SECURITY.md (disclosure policy), CODE_OF_CONDUCT.md, `security.txt`, issue/PR templates.
- [x] 0.8 Provider traits (`KeyEncryptor`, `EmailSender`, `SmsSender`, `Captcha`, `PasswordHasher`) and the internal event bus skeleton.
- [x] 0.9 Tests: `ridm-core` test-support module with mock `KeyEncryptor`/`EmailSender`/`SmsSender`/`Captcha`; testcontainers harness (Postgres + Redis) with a shared fixture that seeds tenant, client, admin, user; cargo-llvm-cov wired into CI with coverage report.
- [x] 0.10 Branch protection on `main`: PRs only, all required status checks in §6 must pass, no bypass; CODEOWNERS.

### Phase 1 — Tenants, users, groups, roles, profile schema
- [x] 1.1 Migrations: users, user_profile_schema, password_history, credentials, groups, group_members, roles, role_assignments, role_composites (indexes lead with tenant_id; RLS policies; `org_id` reserved columns).
- [x] 1.2 Tenant resolver extractor (`/t/{slug}` → cached `Tenant`), RLS `SET LOCAL` per request tx; Redis pub/sub cache invalidation.
- [x] 1.3 Repos + services: tenant CRUD, user CRUD, group CRUD + membership, role CRUD + assignment, effective-roles resolution (user + groups + composites, cached).
- [x] 1.4 Password service: argon2id hash/verify; legacy verifiers (bcrypt, pbkdf2-sha256/512, sha256/sha512 salted, md5 for migration only) with transparent upgrade on successful login; password policy (length, classes, history N, expiry, must_change_password).
- [x] 1.5 Profile schema service: validate attributes on create/update; enforce editable_by; expose schema to UI forms.
- [x] 1.6 Bootstrap: first-run creates master tenant + global admin from env or interactive CLI.
- [x] 1.7 Unit tests (password, policy, role resolution); integration test harness (testcontainers).
- [x] 1.8 Tests: tenant isolation suite v1 (every repo query cross-tenant → empty/404; direct RLS bypass attempt fails); profile schema validation; legacy-hash upgrade-on-login; migration test applying all migrations to seeded snapshot and checking constraints/row counts.

### Phase 2 — Keys, JWT issuance, encryption
- [x] 2.1 Migration: signing_keys. Keygen (RSA-2048/3072, P-256, Ed25519); private keys encrypted via `KeyEncryptor`.
- [x] 2.2 Rotation service: create → activate → retire → revoke with overlap; scheduled rotation job.
- [x] 2.3 JWKS endpoint (cached, ETag). WebFinger endpoint for issuer discovery.
- [x] 2.4 Token service: sign/verify access + ID tokens; claim-mapper pipeline; pairwise `sub` derivation; optional JWE encryption of ID tokens per client.
- [x] 2.5 Refresh token service: opaque, hashed, rotation, family reuse detection; `jti` denylist for instant access-token revocation.
- [x] 2.6 Master key rotation command: re-encrypt all `*_enc` columns under a new key version, zero downtime.
- [x] 2.7 Tests: sign/verify for every alg; rotation overlap (old kid still verifies until retired); revoked kid rejected; JWKS shape and ETag; pairwise `sub` stability; JWE round trip; master-key rotation re-encrypts and verifies; property tests on JWT decoding.

### Phase 3 — OIDC provider core
- [x] 3.1 Migrations: clients (all fields incl. client_type, dual secrets, subject_type, logout URIs), scopes, claim_mappers, consents, refresh_tokens, resource_servers, permissions, permission_assignments. Default scopes seeded per tenant.
- [x] 3.2 Discovery document, complete and accurate for every supported feature.
- [x] 3.3 `/authorize`: full validation (client, exact redirect_uri incl. loopback rules for native, response_type=code, scope, state, nonce, PKCE, prompt incl. `create`, max_age, acr_values, login_hint, ui_locales, `claims` param, `resource` indicators, `response_mode` query|fragment|form_post|jwt); SSO-session check → code or `login_flow` redirect.
- [x] 3.4 `/token`: authorization_code (+PKCE), refresh_token (rotation), client_credentials (service account roles), with client auth basic/post/private_key_jwt; audience-scoped access tokens for resource servers.
- [x] 3.5 `/userinfo`, `/introspect`, `/revoke`, `/end_session` (RP-initiated logout), back-channel logout and front-channel logout.
- [x] 3.6 PAR (RFC 9126), JAR (RFC 9101), JARM.
- [x] 3.7 Dynamic client registration (RFC 7591) and management (RFC 7592) with per-tenant policy (open|initial-access-token|disabled).
- [x] 3.8 Client secret rotation (two active secrets, grace window) and `jwks_uri` refresh.
- [x] 3.9 Integration tests: code+PKCE round trip, refresh rotation + reuse detection, client_credentials, DCR, logout propagation, negative cases.
- [x] 3.10 Security tests: code replay, PKCE missing/mismatched/plain-downgrade, redirect_uri substring/suffix/scheme attacks, loopback port rules, state/nonce mismatch, `id_token_hint` from another tenant, client secret grace expiry, DCR abuse (open policy off), discovery-vs-routes contract test. Fuzz targets: authorize params, redirect_uri matcher.

### Phase 4 — Browser flows (API) and end-user UI
- [x] 4.1 Flow state machine (Redis): identify → authenticate (password | passkey | magic-link | email-otp | sms-otp | upstream IdP) → mfa → step-up check (acr) → profile-completion → terms → consent → done; CSRF bound to flow; retry counters.
- [x] 4.2 Flow API: `GET /flows/{id}` (public state, branding, locale, available methods), `POST /flows/{id}/{step}`, `/cancel`; completion returns `{ redirect_to }`.
- [x] 4.3 Brute-force protection (per-user + per-IP), CAPTCHA challenge via `Captcha` trait after threshold or on registration.
- [x] 4.4 Messaging: `EmailSender` (SMTP via lettre, generic HTTP), `SmsSender` (HTTP webhook), per-tenant settings (encrypted), per-locale templates (handlebars), outbound queue with retry and dead-letter.
- [x] 4.5 Magic link and email OTP login; SMS OTP login; passwordless-only tenant policy.
- [x] 4.6 Self-registration (per-tenant toggle, profile schema driven, email verification, terms acceptance) and invitation acceptance flow.
- [x] 4.7 Recovery/verification: password reset, email verify, resend, temporary password + forced change.
- [x] 4.8 Session policy enforcement: idle/absolute timeouts, max concurrent sessions, trusted-device "remember me" skipping MFA, `prompt`/`max_age`/`acr_values` honored.
- [x] 4.9 i18n: locale negotiation (`ui_locales` → user locale → tenant default), translation bundles, RTL-safe layout.
- [x] 4.10 UI pages: `/login/`, `/register/`, `/invite/`, `/consent/`, `/mfa/`, `/recover/`, `/verify/`, `/logout/`, `/device/`, `/error/`. Query-param driven; tenant theme (logo, colors, custom links, optional custom CSS); mobile-first.
- [x] 4.11 Security notifications to users: new device login, password changed, MFA changed, email changed.
- [x] 4.12 Tests (trusted-device MFA skip is covered with 7.7 once a second factor exists): flow state machine transitions (every step, every error), CSRF on each step, brute-force lockout timing, CAPTCHA gate, magic-link/OTP single-use + expiry, registration with schema validation, invitation acceptance, recovery token single-use, session idle/absolute/concurrent limits, trusted-device skip, locale negotiation, message queue retry/dead-letter with mock senders. Playwright e2e: password login, magic link (via Mailpit), registration, recovery, consent, logout; extended after 5.1 with email OTP, invitation acceptance, forced password change, profile completion, terms re-acceptance and lockout.

### Phase 5 — Admin API
- [x] 5.1 Admin auth middleware + admin permission model (`ridm:tenants:*`, `ridm:users:read|write`, `ridm:clients:*`, `ridm:keys:*`, `ridm:audit:read`, …) with built-in admin roles: owner, admin, user-manager, client-manager, viewer. Built as: per-tenant built-in resource server `urn:ridm:admin` + permission catalogue + `ridm:*` roles seeded by migration (Rust mirror in `services/admin_access.rs`, contract-tested); `AdminCtx` bearer extractor (audience-bound, session-bound, permissions re-resolved per request, master = global scope); `GET /admin/me`, `GET /admin/permissions`.
- [x] 5.2 Tenants: CRUD, settings (password, session, MFA, registration, locale, branding, captcha, ip rules, feature flags). Built as: `/admin/tenants` list (global: all, paginated; tenant-scoped: own), create/delete global-only, get/patch per scope; settings via JSON merge patch with unknown-field rejection; `features` flag map in settings; captcha provider config (encrypted, secret redacted on read); `AdminTenantPath` extractor admits disabled tenants; problem+json `Json` extractor. IP rules are their own resource in 5.10.
- [x] 5.3 Clients: CRUD, type-driven defaults, secret generate/rotate (reveal-once), URIs, grants, scopes, audiences, token settings, encryption, logout URIs, CORS, service account. Built as: `/admin/tenants/{slug}/clients` (id or public `client_id` in the path); merge-patch updates plus `status`; scopes/audiences validated against the tenant; `secrets` metadata on reads; rotate with grace and revoke by secret id; switching auth method to/from secrets drops or mints one (also on RFC 7592 PUT); service account = a `svc-<client_id>` user linked to the client; registration-token issue for RFC 7592 management.
- [x] 5.4 Users: cursor-paginated list/search, CRUD, set/temp password, force reset, enable/disable/unlock, sessions + revoke, credentials, trusted devices, role/group assignment, consents. Built as: `/admin/tenants/{slug}/users` and sub-resources; create accepts `password` or `temporary_password` (returned once); `PATCH` with `null` clears, `status` limited to active/disabled (disable revokes sessions); password route with policy override, notify and session revocation; credentials = password summary plus `credentials` rows (metadata only, `repos::credentials`); role and group grants pass `require_can_grant` over the composites-expanded (and ancestor-group) permissions via `admin_access::permissions_of_grant`. Deferred to where their storage arrives: PATs (8.5), federated identities (8.3), per-user audit (5.9).
- [x] 5.5 Groups, roles, resource servers + permissions, scopes, claim mappers CRUD. Built as: `/admin/tenants/{slug}/{groups,roles,resource-servers,scopes,claim-mappers}`; `built_in` resource servers, their catalogue and built-in roles are immutable (403); every grant path (role to user/group, group membership, composite child, admin-catalogue permission to role) passes `require_can_grant` over `admin_access::permissions_of_grant`; permission grant/revoke and resource-server/permission deletion bump the roles version; claim mappers are cached per client under a tenant-wide mappers version (`claim_mappers::bump_mappers_version`) so tenant-wide changes reach every client at once; new `scope.*`, `claim_mapper.*`, `resource_server.*`, `permission.*` events. Identity provider CRUD moves to 8.3 with its table and brokering semantics.
- [x] 5.6 Keys: list, rotate, revoke. Master-key rotation status. Built as: `/admin/tenants/{slug}/keys` list (status filter), create (pending or activated), rotate, get, activate, retire, revoke; `/admin/master-key` status with pending row count and `/rotate` re-encryption, both global-only.
- [x] 5.7 Invitations: create/list/revoke/resend. Bulk user import (CSV/JSON, legacy hashes) and export. Built as: `/admin/tenants/{slug}/invitations` (token only by email, resend rotates it, role/group invitations guarded by `require_can_grant`); `POST .../users/import` (JSON or CSV, `dry_run`, per-row report, legacy hashes via `password::import_hash`, roles/groups by name; `services/bulk_users.rs`) and streaming `GET .../users/export` in JSON or CSV.
- [x] 5.8 Messaging admin: SMTP/SMS settings with test-send, template CRUD per locale with preview. Built as: `/admin/tenants/{slug}/messaging/{email,sms}` (encrypted provider settings, secrets redacted as `*_set`, omitted secret kept, validation of security/URLs), `/{email,sms}/test` straight through the configured sender, `/templates` catalogue + `/{channel}/{event}/{locale}` override GET/PUT/DELETE (built-in shown as starting point, rendering validated) + `/preview` (stored or draft, sample vars), `/log` without bodies and `/log/{id}/redeliver`; `services/messaging.rs`.
- [x] 5.9 Audit: list/filter/export; retention policy per tenant. Migration for partitioned `audit_events` with hash chain. Includes the per-user audit route under `/admin/tenants/{slug}/users/{id}/audit` deferred from 5.4. Built as: migration `20260915144648_audit_events` (range-partitioned by month with a default partition, `audit_ensure_partitions()`, tenant RLS, per-chain `seq`), `services/audit.rs` writer subscribed to the event bus (per-chain advisory lock, `SHA-256(prev_hash || canonical row)`), list/export/verify per tenant and for the global chain, `settings.audit.retention_days`, daily `audit_retention` job purging an expired chain prefix and creating partitions.
- [x] 5.10 Webhooks CRUD, delivery log, redeliver. IP rules CRUD. Built as: migration `20260915150001_webhooks_ip_rules` (`webhooks` with encrypted HMAC secret + `key_version`, `webhook_deliveries` queue, `ip_rules`, all under tenant RLS); `services/webhooks.rs` (event patterns, reveal-once secret on create/rotate, bus dispatcher → queued deliveries, `deliver_due` with HMAC `X-RIDM-Signature`, retry/backoff/dead-letter, redeliver, test ping) + `webhook_delivery` job every 30 s; `services/ip_rules.rs` with CIDR normalization (enforcement stays in 9.2); webhook secrets registered for master-key rotation; dispatcher started in main and the test harness.
- [x] 5.11 Tenant export/import (config as code): deterministic JSON, secrets excluded or encrypted, idempotent apply with diff preview. Built as: `services/tenant_config.rs` with the `ridm.tenant/1` document (sorted collections, natural keys: client `client_id`, role `name` or `client_id/name`, permission `identifier#name`, group `path[]`, template `channel/event/locale`), `export`, `plan` (normalizes the desired document, compares against a fresh export, field-level diffs, deletes only with `prune`, built-in roles/servers and standard scopes never deleted) and `apply` (dependency-ordered, per-item errors, secrets of created clients and webhooks reported once); `GET .../export`, `POST .../import`. Secrets are excluded (not encrypted): provider credentials are configured per environment.
- [x] 5.12 Cache invalidation on every write; utoipa docs complete; generated TypeScript admin client published from OpenAPI. Built as: invalidation audit (every cached key has an eviction on its write path; roles/mappers versions cover derived caches); `#[utoipa::path]` on every admin handler with `ToSchema`/`IntoParams` on all models, routers as `utoipa_axum::OpenApiRouter` merged in `openapi.rs` (`/openapi.json`, Swagger UI at `/docs` with `DOCS_ENABLED`, `ridm-api openapi` CLI); committed `api/openapi.json` kept current by a test; `ui/lib/api/openapi.d.ts` generated by `npm run gen:api` (openapi-typescript via npx with TypeScript 5) and `ui/lib/api/client.ts` on `openapi-fetch` with bearer and 401 middleware.
- [x] 5.13 Tests: admin permission matrix (each built-in role × each endpoint), tenant isolation suite v2 (every admin endpoint cross-tenant), cursor pagination stability, bulk import (legacy hashes, bad rows reported), export/import round trip is idempotent and diff is empty, audit hash chain verifies, TypeScript client contract test against live OpenAPI. Built as: `admin_matrix.rs` derives every operation and its required permission from the route sources (`#[utoipa::path]` + first `admin.require*`) and checks all five built-in roles, the global owner and anonymous callers, then calls every tenant-scoped operation across tenants; `admin_pagination.rs` walks user and client listings with mid-walk inserts; bulk import (`admin_bulk_users.rs`), export/import (`admin_tenant_config.rs`) and audit chain (`admin_audit.rs`) suites from their sub-phases; `ui/e2e/openapi-contract.spec.ts` compares the live document with the committed one and drives the typed client against the live API.

### Phase 6 — Admin UI
- [x] 6.1 Shell: OIDC PKCE login, refresh, tenant switcher, global search, sidebar, dark/light, keyboard shortcuts. Built as: the console is served at `/console/` (`/admin/*` is the API); API side, a built-in public client `ridm-admin-console` per tenant (`services/admin_console.rs`: PKCE, no consent, `urn:ridm:admin` audience, redirect URIs from `UI_URL`, ensured at startup and on tenant create, undeletable, excluded from export/import/prune) and tag-prefixed unique OpenAPI operation ids (the generated client had been collapsing same-named handlers); UI side, `lib/console/auth.ts` (PKCE start/callback/refresh/logout, tokens in `sessionStorage`), `lib/console/session.tsx` (external session store read through `useSyncExternalStore`, typed client with shared proactive refresh and 401 fallback, `/admin/me` identity), `components/console/shell.tsx` (sign-in gate, sidebar filtered by permissions, top bar, phone drawer, theme switch, shortcut help), `palette.tsx` (⌘K search over pages/users/clients, tenant switcher for global admins; tenant travels as `?tenant=`), `theme.tsx` (system/light/dark with pre-paint restore), `shortcuts.tsx`; `/console/` overview page; `admin_console.rs` test suite and `e2e/console.spec.ts` (global setup promotes the e2e user to global owner). Navigation entries are added as their pages land.
- [x] 6.2 Tenants: list/create; settings pages (auto-save, all fields), branding editor with live login-page preview. Built as: `/console/tenants/` (all tenants for global admins, client-side filter, create dialog with slug validation → new tenant's settings) and `/console/settings/` (`components/console/settings/*`: general incl. feature flags, sign-in methods/MFA policy/registration, password policy/lockout/CAPTCHA policy + provider sub-form, sessions & tokens, branding, locale & notices, keys/discovery/DCR/audit, danger zone); `lib/console/autosave.ts` (`useAutoSave`: coalesced merge patches, 600 ms debounce capped at 2.5 s, keepalive flush on hide/unload, error → reload stored state) with `SaveIndicator`; console form controls in `components/console/form.tsx` (`Field`, `TextInput`, `NumberInput`, `Toggle`, `SelectInput`, `TagsInput`, `ColorInput`, `TextArea`); branding editor frames the real login page in preview mode (`/login/?tenant=&preview=1` renders `Authenticate` on a stand-in flow, `TenantProvider preview` takes `ridm:preview` postMessage overrides after posting `ridm:preview:ready`); `e2e/console-tenants.spec.ts`.
- [x] 6.3 Clients: table, create wizard (type → grants → URIs → scopes/audiences), detail with auto-save sections, secret reveal-once + rotate, "playground" to run the client's flow end to end. Built as: `/console/clients/` (`components/console/clients/`: `table.tsx` with server prefix search and cursor paging via `useInfiniteQuery`, `wizard.tsx` four steps with `typeDefaults` mirroring the API, `detail.tsx` sections on `useAutoSave` over the metadata merge patch plus secrets/service-account/registration-token/delete mutations, `pickers.tsx` scope and audience check lists, `reveal.tsx` reveal-once modal + copy button); `/console/playground/` with `lib/console/playground.ts` (PKCE run state in sessionStorage across the redirect, token/userinfo requests with the client's auth method, JWT decoding); `lib/console/hooks.ts` (`useDebounced`, `useScopes`, `useResourceServers`); Clients joins the navigation (`g c`); `e2e/console-clients.spec.ts`.
- [x] 6.4 Users: virtualized table with search/filters, user detail (profile per schema, credentials, devices, sessions, PATs, roles, groups, identities, consents, audit), invite dialog, bulk import/export. Built as: `/console/users/` (`components/console/users/`: `table.tsx` windowed rows via `lib/console/virtual.ts` with server prefix search, status filter, include-deleted and scroll-driven paging; `create.tsx` with reveal-once temporary password; `invite.tsx` invite dialog + open-invitations list with resend/revoke; `import.tsx` JSON/CSV paste-or-file with dry run report and export download through the console token; `detail.tsx` tabs — profile (identity fields + `attributes.tsx` schema-driven fields, full attribute set on every save), password & credentials, sessions & devices, roles, groups, consents, audit); API: new `GET/PUT /admin/tenants/{slug}/profile-schema` routes (test in `admin_tenants.rs`) and a Profile attributes editor in Settings (`settings/profile.tsx`, whole-schema PUT on auto-save); PATs and identities are placeholders until 8.5/8.3; Users joins the navigation (`g u`); `e2e/console-users.spec.ts`.
- [x] 6.5 Groups (tree), roles, resource servers + permissions, scopes, claim mappers, identity providers (with test-connection). Built as: `components/console/access/` — `groups.tsx` (tree with `role=tree`, create at any level, parent select excluding descendants, JSON attributes, group roles, members via user-search `Picker`), `roles.tsx` (realm/client roles, composites, permission grants from a catalogue built over every resource server, holders resolved to users/groups, built-in read-only), `resource-servers.tsx` (fields + permissions, `urn:ridm:admin` read-only), `scopes.tsx` (description, claims, resource server binding, default flag; standard scopes undeletable), `mappers.tsx` (kind-specific fields via `MapperFields`, include-in targets, whole-config PATCH on auto-save); shared `common.tsx` (`CreateDialog`, `DeleteButton`, `Split`); `lib/console/access.ts`; five route pages; navigation entries (`g g`, `g r`, `g a`, `g p`, `g m`); `e2e/console-access.spec.ts`. Identity providers (with test-connection) move with their CRUD to 8.3.
- [x] 6.6 Keys page (timeline, rotate), audit viewer (filters, export), webhooks (deliveries, redeliver), IP rules, messaging (settings, templates with preview, test-send). Built as: `components/console/ops/` — `keys.tsx` (timeline bars per key, rotate, create pending/activated, activate/retire/revoke, JWK viewer, master-key status + re-encrypt for global admins), `audit.tsx` (tenant or global chain, filters with `datetime-local` → RFC 3339 `Z`, cursor stack for newer/older, expandable rows, JSON/CSV export through the console token, verify), `ip-rules.tsx` (inline add/edit/delete; uses `ridm:tenants:*` like the API), `webhooks.tsx` (list/detail with auto-save, reveal-once secret on create/rotate, test ping, deliveries with status filter, 10 s refresh and redeliver), `messaging.tsx` (email/SMS provider forms with write-only secrets and test send, template picker + editor with debounced draft preview and opt-in HTML rendering, override/reset, outbound log with redeliver); `lib/console/ops.ts` (event-name catalogue, `downloadWithToken`); navigation groups Security and Integrations; `e2e/console-ops.spec.ts` (with an afterAll that removes any template override left by a failed run, since the registration emails of other specs depend on the built-in).
- [x] 6.7 Dashboard: logins/day, active sessions, failed logins, MFA adoption, top clients (recharts). Built as: new `services/stats.rs` + `routes/admin/stats.rs` (`GET /admin/tenants/{slug}/stats?days=`, `ridm:tenants:read`; per-day sign-ins/failures from `login_attempts`, live sessions from `sso_sessions`, user counts and TOTP/passkey enrolment from `credentials`, top clients from `authorization.granted` audit rows; `admin_stats.rs` test); `components/console/dashboard.tsx` on the overview page (stat tiles, recharts line chart with legend, tooltip and table view, horizontal bar chart of top clients with direct labels, users card with an adoption progress bar; series colours `--series-1/2` validated with the dataviz palette script in light and dark).
- [x] 6.8 Tenant export/import UI with diff preview. Built as: `components/console/config.tsx` at `/console/config/` — export download and load-into-editor, file or paste import, prune toggle, `dry_run` plan grouped by resource with op badges and field diffs (long values collapse), apply with per-item errors and reveal-once secrets of created clients/webhooks; navigation entry (`g x`); `e2e/console-config.spec.ts`.
- [x] 6.9 Playwright e2e: admin login, tenant create + settings auto-save, client wizard + secret reveal, user CRUD + invite, role/group assignment, key rotate, audit filter, export/import with diff. Axe accessibility checks on every admin page. Built as: the journeys landed with their sub-phases (`console`, `console-tenants`, `console-clients`, `console-users`, `console-access`, `console-ops`, `console-config` specs, every one with axe checks on the pages it touches); `console-a11y.spec.ts` sweeps every console page in its landing state (light, dark, phone width with an overflow check); findings fixed along the way: header search button name, link colour derived from the tenant accent (`--link` = accent mixed toward black/white per theme, used by the end-user pages too), cards clipped to their grid cell, users table scrolling sideways on phones. Full suite: 84 Playwright tests and 61 Rust test binaries green. Phase 6 complete.

### Phase 7 — MFA and credential hardening
- [x] 7.1 TOTP: enroll (QR), verify, recovery codes (hashed). Built as: `services/totp.rs` (RFC 6238 SHA-1/6/30 with one step of skew via `totp-rs`; secret encrypted in a `totp` credential row under AAD `credentials:{tenant}:{id}`; ten `xxxxx-xxxxx` recovery codes SHA-256 hashed inside one encrypted `recovery_code` row, single-use; per-step replay guard in Redis; pending enrolment in Redis bound to the flow; `MfaChanged` events and `mfa_changed` notices); flow steps `POST /flows/{id}/mfa/totp/enroll` (secret + otpauth URI), `/mfa/totp/confirm` (proof code → `{recovery_codes, flow}`), `/mfa/verify` (app code or recovery code, `remember_device`), five wrong codes discard the flow; `PublicFlow.mfa` (`factors`, `enroll`, `recovery_codes`); `flows::mfa_required` now evaluates the tenant policy (`required` enrols on first sign-in, `optional` asks enrolled users, trusted device skips, `acr_values` step-up always asks; role modes behave as `optional` until 7.4) and a passed factor refreshes the session with `amr` + `acr` (`ACR_MFA` or the requested class); `/mfa/` page: enrol (QR rendered client-side with `qrcode`, manual key, device label), verify, recovery-code mode, recovery codes shown once with copy/download; `api/tests/mfa_totp.rs`, matrix steps, `ui/e2e/mfa.spec.ts` (step-up driven so other specs stay untouched).
- [x] 7.2 WebAuthn/passkeys: register, authenticate, passwordless + discoverable credentials, multiple authenticators with labels. Built as: `services/passkeys.rs` on `webauthn-rs` 0.5 (RP id = UI host or the tenant's custom domain, the API origin accepted when it shares the host; user verification required; serialised `Passkey` encrypted in a `webauthn` credential row with the credential id in the new `credentials.external_id` column, unique per tenant and type; ceremony state in Redis for five minutes bound to the flow, spent by the first answer; counter and backup flags stored after every assertion); `services/credential_secrets.rs` holds the row encryption TOTP and passkeys share. Flow steps: `POST /flows/{id}/passkey/start|finish` (discoverable sign-in at the authenticate stage, `amr` `hwk`+`user`+`mfa` and the MFA `acr` on the session, so no second step follows), `/mfa/passkey/register` + `/register/finish` (label; the first second factor issues the recovery codes; `{recovery_codes, flow}` wrapper as for TOTP), `/mfa/passkey/start|finish` (second step); refusals answer `401 invalid_passkey`. `PublicFlow.mfa.factors` lists `webauthn`; all passkey offers follow the tenant's `auth.passkey` toggle (verifying an existing passkey always works). UI: `lib/passkeys.ts` over `@simplewebauthn/browser` with translated failure codes; login page "Sign in with a passkey" (primary when it is the only method); `/mfa/` chooses app or passkey on first enrolment, verifies in app, passkey or recovery mode; console shows credential kinds by name. Tests: `api/tests/passkeys.rs` with the `webauthn-authenticator-rs` software passkey (enrolment, exclusion of enrolled keys, replay refused, discoverable sign-in incl. wrong user handle, tenant toggle), flow matrix steps, `ui/e2e/passkey.spec.ts` on a CDP virtual authenticator.
- [x] 7.3 Email OTP and SMS OTP as second factors (reusing 4.5 senders). Built as: `services/otp_factors.rs` over the passwordless module's code store (`store_code`/`check_code`/`recently_issued` extracted from it: six digits hashed in Redis, bound to the flow, single-use, ten minutes, five attempts; sends limited to three per ten minutes per user, a repeat inside a twenty-second cooldown reuses the pending code); tenant `settings.mfa_methods {totp, email_otp, sms_otp}` decides what the `mfa` stage offers (passkeys follow `auth.passkey`), `PublicFlow.mfa.methods` lists it and `mfa.phone` carries the masked number an SMS enrolment would use. Flow steps: `POST /flows/{id}/mfa/{email|sms}/enroll` (a code to the address, or to the `phone` given in E.164 when the account has none; switching numbers discards the pending code), `/confirm` (stores one `email_otp`/`sms_otp` credential row whose encrypted material is the proven destination, marks the address/number verified, saves a new number, first factor issues the recovery codes), `/send` and `/verify` for later sign-ins (to the account's current verified address/number; `amr` `otp`, plus `sms`); `totp::Factors` and `SECOND_FACTOR_KINDS` count the new kinds, the dashboard's adoption stat too; `mfa/totp/enroll` honours `mfa_methods.totp`. UI: `/mfa/` chooser lists every offered method (auto-picks a lone one), `EnrolOtp` sends at once (or asks for a number), verify mode adds email/SMS with "Send again"; console Sign-in section gains the "Second-step methods" toggles. Tests: `api/tests/mfa_otp.rs` (email enrol + later verify + replay, SMS with a new number and the number-switch guard, toggles, optional policy), flow matrix steps, `ui/e2e/mfa-otp.spec.ts` (Mailpit).
- [x] 7.4 Tenant MFA policy: off | optional | required | required-for-roles | required-for-admins; step-up by `acr_values`; `amr`/`acr` claims accurate. Built as: `flows::mfa_required` evaluates `required_for_roles` (any listed role among the user's effective roles) and `required_for_admins` (any admin-console permission via `admin_access::permissions_of_user`), both falling back to the `optional` rule; `flows::{is_mfa_acr, requested_mfa_class}` define an MFA class as one ending in `:mfa`; `/authorize` treats only MFA classes as demands (other `acr_values` are voluntary and the session's class is returned) and sends a fresh session that merely lacks the class straight into a flow at the `mfa` stage (`/mfa/` page, existing session and `amr` carried over; `prompt=none` → `login_required`; `max_age`/`prompt=login` still re-authenticate in full); `amr`/`acr` were already recorded per factor and are now covered by tests end to end (ID-token claims after a single factor, after a step-up, on a trusted device). Tests: `api/tests/mfa_policy.rs`, updated `authorize.rs` expectation, `mfa.spec.ts` step-up on a live session; console hint and README.
- [x] 7.5 Breached-password check via k-anonymity (HIBP-compatible, pluggable, off by default for air-gapped installs). Built as: `ridm_core::providers::BreachChecker` (works on the upper-case SHA-1 hex; `password_sha1_hex`) with `test_support::MockBreachChecker`; `services/breach.rs` `HibpChecker` (five-digit prefix to `{BREACH_CHECK_URL}/{prefix}` with `Add-Padding: true`, suffix matched locally, five-second timeout); `Config.breach_check_url` from `BREACH_CHECK_URL` (default HIBP; `off` = `AppState.breach` is `None`); `password::set_password` checks after the policy rules and before history when `password.check_breached` is on (skipped by `skip_policy`), a hit is a `password` field error, a lookup failure is logged and accepted; console hint. Tests: `api/tests/breach.rs` (tenant opt-in, order with policy rules, admin override, registration surface, deployment off, outage, the client against a local range endpoint incl. padding and prefix-only requests).
- [x] 7.6 UI: `/mfa/` flow page, account console MFA + passkeys + trusted devices management. Built as: the `/mfa/` flow page from 7.1–7.3; migration `20260916131542_account_audience` seeds the built-in `urn:ridm:account` resource server per tenant (trigger + backfill); `services/account_console.rs` seeds the `ridm-account-console` PKCE client per tenant (shared `admin_console::{ensure_builtin, for_every_tenant, is_builtin_client}`); `middleware/account.rs` `AccountCtx` (bearer with the account audience, issued by the path's tenant, live session, own user only; `require_recent_auth(needs_mfa)` = `auth_time` within fifteen minutes plus an MFA `acr` once a factor exists, else `AppError::ReauthenticationRequired` → `403 urn:ridm:error:reauthentication-required`); `routes/account/{me,mfa,devices}.rs` in the OpenAPI document (tag `account`): `GET me`, `GET mfa`, `POST mfa/totp/enroll|confirm`, `mfa/passkey/register[/finish]`, `mfa/{email|sms}/enroll|confirm` (`otp_factors` now takes a `Scope {id, ui_locales}`; the console scopes ceremonies to the SSO session), `DELETE mfa/credentials/{id}` (recovery codes dropped with the last factor), `POST mfa/recovery-codes`, `GET devices`, `DELETE devices[/{id}]`. UI: `lib/console/auth.ts` became `createAuth(app)` (the admin console's names re-exported), `lib/session-store.ts` is the generic session store both consoles use, `lib/account/session.tsx` (`useAccount`, `needsReauth`, `reauth(mfa)` re-authorizes with `max_age=0` and the MFA class), `/account/` (sign-in card with the tenant from `?tenant=`, security page: factors with add/remove, recovery codes renew, trusted devices forget/forget all), `/account/callback/`, shared `components/mfa/{recovery-codes,totp-setup}.tsx`. Tests: `api/tests/account.rs` (token rules, recent-auth rule, enrol/remove/renew, devices, client seeding), `ui/e2e/account.spec.ts`; README, e2e README.
- [x] 7.7 Tests: TOTP drift window, recovery code single-use, WebAuthn register/authenticate with virtual authenticator (Playwright CDP), OTP factors, MFA policy matrix (off/optional/required/for-roles/for-admins), step-up `acr` enforcement, `amr` accuracy, HIBP check with mock. Playwright e2e: TOTP enroll + login, passkey enroll + passwordless login. Where each lives: TOTP drift window and replay in `mfa_totp.rs` (`later_sign_ins_verify_with_the_app_within_the_drift_window_and_never_twice`, one step either side accepted once, two steps away refused), recovery codes single-use in any spelling in `mfa_totp.rs`, WebAuthn register/authenticate/discoverable/replay with the software authenticator in `passkeys.rs` and with the CDP virtual authenticator in `e2e/passkey.spec.ts`, OTP factors in `mfa_otp.rs` and `e2e/mfa-otp.spec.ts`, the policy matrix (`the_policy_matrix`: every mode against a plain user, an enrolled one, a role holder and an administrator) plus role/admin detail and the live-session step-up in `mfa_policy.rs`, `acr`/`amr`/`auth_time` accuracy in ID tokens after one factor, after a step-up, on a trusted device and after a passkey sign-in in `mfa_policy.rs` and `passkeys.rs`, the breach check with `MockBreachChecker` and a local range endpoint in `breach.rs`, the account console in `account.rs`; e2e TOTP enrol + login (`mfa.spec.ts`), passkey enrol + passwordless login (`passkey.spec.ts`), account console (`account.spec.ts`). Phase 7 complete.

### Phase 8 — Account console, identity brokering, device flow, PATs
- [x] 8.1 Account console UI (`/account/`): profile per schema (auto-save), password change, email/phone change with verification, MFA, passkeys, trusted devices, sessions (revoke, sign out everywhere), linked identities, consented apps, personal access tokens, data export, account deletion. Built as: `components/account/shell.tsx` gains a tab bar (Profile `/account/`, Security `/account/security/`, Applications `/account/apps/`, Your data `/account/data/`; the sign-in card returns to the page it was opened from); `profile.tsx` (`useProfile`, the form keyed by the stored document, `AttributeField` per declared attribute with `editable_by !== user` disabled, the locale from the tenant's list, `useAutoSave` sending the whole user-editable attribute set as the API replaces it, a refused save rebuilds the form from the server); `contact.tsx` (email and phone rows with verified badges and the pending change with "enter the code" and cancel, the two-step change dialog reused for both, phone removal); `password.tsx` (state and policy, the change dialog with current password when one is set, repeat, "sign out everywhere else", a done notice counting the sessions ended); `sessions.tsx` (this browser marked, one ended on its own or all but this one); `apps.tsx` (logo, scopes, privacy and terms links, remove access); `data.tsx` (the export fetched with the bearer token and saved through the `Content-Disposition` name, deletion behind a username-typed dialog that ends the console session); `lib/account/session.tsx` `refreshMe()` after contact changes; `useProblemText` and `useSecurityChange` (re-authentication) shared from `security.tsx`; strings under `account.*`. Linked identities and personal access tokens join the console with 8.3 and 8.5. Tests: `ui/e2e/account-profile.spec.ts` (schema-shaped profile saved and reloaded with the admin-only field read-only, contact details, sessions with a second browser signed out from here both ways, applications page, the export download parsed, the deletion dialog), `account.spec.ts` moved to the security page; e2e README.
- [x] 8.2 Account API (self-scoped): all of the above; GDPR export (JSON) and deletion (soft-delete → purge job). Built as (before 8.1, which renders it): `AccountCtx::require_recent` (recent sign-in, the second step once a factor exists) shared by every security change; `routes/account/profile.rs` `GET/PATCH profile` (declared attributes with values, the schema in form order, the locale checked against the tenant's, pending contact changes; the patch goes through `users::update` as `Actor::User`, so `editable_by` and required rules apply and untouched admin-only attributes are kept); `password.rs` `GET/PUT password` (current password verified via `verify_and_upgrade` while one is set, policy and breach checks via `set_password`, `sign_out_others` ends the other sessions and their refresh tokens); `services/contact_changes.rs` + `contact.rs` (`POST {email|phone}/change` sends a six-digit code to the new destination over the `otp` template with the passwordless code store, send limit and twenty-second cooldown, a different destination discards the pending code, an address of another account is `409`; `POST {email|phone}/confirm` moves the account over verified (the user service's `email_changed` notice reaches the old address), wrong codes are `code` field errors and an exhausted code drops the change; `DELETE {email|phone}/change` cancels; `DELETE phone` refuses while an SMS factor uses the number); `sessions.rs` (`GET sessions` newest first with `current`, `DELETE sessions/{id}`, `DELETE sessions[?keep_current]`, refresh tokens revoked with each session); `services/account.rs` + `apps.rs` (`GET apps` joins live consents with the client's name, logo and policy links, `DELETE apps/{client_id}` revokes the consent and the client's refresh tokens); `data.rs` `GET export` (`AccountExport`: user record without secrets, credential metadata, devices, sessions, consents, effective roles, groups, the audit trail as actor or subject capped at 1000, offered as a `Content-Disposition: attachment` download) and `DELETE me` (username typed as `confirm`; refused by `settings.account.self_deletion = false` and for users holding any admin permission; revokes sessions, tokens and devices, then `users::delete` soft-deletes); `TenantSettings.account: AccountPolicy {self_deletion: true, deletion_retention_days: 30}`; `repos::users::purge_deleted` + `jobs/user_purge.rs` (daily, leader-locked, every tenant, cascades) in the scheduler. Linked identities (8.3) and personal access tokens (8.5) join the account API with their phases. Tests: `api/tests/account_self.rs` (schema-shaped profile, password change rules and sign-out-elsewhere, email and phone changes end to end with the mocks incl. cooldown, conflict, cancel and removal, sessions, consented apps and the consent asked again, export contents and header, deletion incl. confirmation, administrator and policy refusals, name reuse, purge after retention).
- [x] 8.3 (also adds the admin identity-provider CRUD deferred from 5.5 and the `users/{id}/identities` list/unlink routes deferred from 5.4) Upstream OIDC/OAuth2 providers: per-tenant config, `/broker/{alias}/start|callback`, discovery + JWKS cache, attribute mappers, account linking (auto by verified email | explicit | always-new), first-login profile completion. Presets: Google, Microsoft, GitHub, Apple, GitLab. Built as: migration `20260916153515_identity_providers` (`identity_providers` with the client secret encrypted under the master key and registered in `master_key::TABLES`, `federated_identities` keyed by provider and subject, one identity per provider per user); `models/identity_provider.rs` (`IdpKind`, `LinkPolicy`, `IdpAuthMethod`, `IdpMappers` with claim paths and attribute mappings, `NewIdentityProvider`/`IdentityProviderUpdate` merge patch with `client_secret: null` clearing, `PublicIdentityProvider`, `LinkedIdentity`); `services/identity_providers.rs` (`PRESETS` for Google, Microsoft, GitHub, Apple and GitLab with hints, alias and endpoint validation — https, or http on loopback for dev and tests —, `discover` with Microsoft's `common` issuer matching, `finish` completing a row from preset + input + discovery, CRUD with cache invalidation and events, `offered` cached for the login page, the JWK set cached an hour with a throttled refresh on unknown `kid`, `get_json`/`post_form` helpers); `services/broker.rs` (`start` with state record, nonce and PKCE in Valkey and Apple's `form_post`; `callback` → `prove` (token exchange with basic, post or PKCE-only auth, ID-token verification against the provider's keys incl. issuer, audience, nonce, `azp`; userinfo merged under the ID token, GitHub's `/user/emails`), `resolve_user` by link policy with both-sides-verified email linking, `create_user` with username fallbacks (`mapped → email → alias-subject`, random suffix on conflict) and `NewUser.defer_required` so a required attribute is collected at the profile step, `apply_mappers` on every sign-in, `complete_authentication` with `amr: ["fed"]`, `BrokeredLogin`/`IdentityLinked`/`IdentityUnlinked` events; link tickets for the account console; `BrokerError` codes carried to the login page as `?broker_error=` and to the console as `?link_error=`); `routes/broker.rs` (`GET start?flow=|ticket=`, `GET|POST callback`, the UI page chosen by the flow's stage); `routes/admin/identity_providers.rs` (`ridm:idps:*`, `presets`, `discover`, CRUD by id or alias, every answer with the `callback_url`); users `GET/DELETE identities`; `routes/account/identities.rs` (`GET identities` with the providers still available, `POST identities/link` → one-time URL, `DELETE identities/{idp_id}`); `PublicFlow.identity_providers`; providers in the tenant export/import without secrets (`report.secrets.identity_providers` lists those to set). UI: login page "Continue with …" buttons and `broker_error` messages; account security page "Linked accounts" (link through the provider, unlink, `?linked=1`/`?link_error=`); console `/console/identity-providers/` (create dialog with presets or an issuer, detail editor with callback URL, endpoints, client and secret, scopes, PKCE, link policy, trusted email, mappers; `g d`); user detail lists linked identities with unlinking. Tests: `api/tests/broker.rs` (mock OIDC provider issuing ES256 ID tokens and a GitHub-like OAuth 2.0 provider mounted in the test app: first sign-in creates and links, reuse, mapped attributes, verified-email linking rules, explicit and always-new policies, state replay, denial, nonce and issuer mismatches, a flow past its first step, hidden providers, first-login profile completion, userinfo identity, account link/unlink with tickets, admin CRUD with secrets never returned, confinement and role checks, admin identities), `admin_tenant_config.rs` (export/import), `ui/e2e/broker.spec.ts` (rIDM as its own upstream); README, e2e README.
- [x] 8.4 Device authorization grant (RFC 8628): `/device_authorization`, `/device/` UI, polling on `/token`. Built as: migration `20260916163512_device_codes` (audit rows: pending → approved/denied → consumed); `services/device_codes.rs` (records in Valkey keyed by the device code's hash with the user code pointing at them, `XXXX-XXXX` user codes from a look-alike-free alphabet claimed with `SET NX`, `find_by_user_code` with a per-address guess limit (10 per 10 min → 429), `approve`/`deny` rewriting the record in place with `KEEPTTL`, `poll` enforcing the interval (`slow_down` adds five seconds) and spending an approval with `GETDEL`); `oidc/device.rs` `POST /t/{slug}/device_authorization` (client auth like `/token`, the grant must be allowed, scopes and `resource` indicators validated as at `/authorize`; `discovery.CAPABILITIES.device = true`); `routes/device.rs` `POST /t/{slug}/device/verify {user_code}` creating a login flow for the device's client with `AuthRequest.device_code` set and the device page as its redirect URI (a signed-in browser is advanced straight to consent or done); the flow's `finish` approves the device code and redirects to `/device/?done=1`, `cancel` and a consent denial mark it denied (`?error=access_denied`); the token endpoint's `device_code` grant maps `poll` to `authorization_pending`, `slow_down`, `access_denied`, `expired_token` or issues tokens from the approving session; the `/device/` page shows the approved and denied states and a clearer message for an unknown code. Tests: `api/tests/device_flow.rs` (response shape, pending and slow-down, approval through password and consent, tokens once, audit row, denial, cancellation, expiry, endpoint refusals, another client's code, guess limiting, a signed-in browser skipping to consent or done, discovery); README.
- [x] 8.5 Personal access tokens: create/list/revoke, usable as bearer with scoped permissions. Includes the admin `users/{id}/pats` list/revoke routes deferred from 5.4. Built as: migration `20260916165628_personal_access_tokens` (hash unique, scopes, expiry, last use, revocation); `models/personal_access_token.rs` (`PAT_SCOPE_ACCOUNT`, `NewPersonalAccessToken {name, scopes, expires_in_days}`, `CreatedPersonalAccessToken` with the token once); `services/personal_access_tokens.rs` (`rpat_` tokens hashed with SHA-256, `available_scopes` = `account` + the user's admin permissions, `create` validating name, held scopes and the tenant's maximum lifetime — `AccountPolicy.personal_tokens` and `personal_token_max_days` — `authenticate` resolving a bearer through a bypass lookup then checking the tenant, the user and narrowing the permissions to what the user still holds, `last_used_at` touched at most once a minute via a Valkey claim, `revoke_all_for_user` on account deletion, `find` for introspection); `AdminCtx::from_personal_token` (client `pat`, no session) and the `AccountCtx` branch (`account` scope, `amr: ["pat"]`, no `auth_time`, so nothing needing a recent sign-in); introspection reports `personal_access_token`; `routes/account/tokens.rs` (`GET tokens` with available scopes and the tenant's policy, `POST tokens` and `DELETE tokens/{id}` behind `require_recent`); admin `GET/DELETE users/{user}/pats[/{id}]`; the account export lists tokens and identities. UI: account security page "Personal access tokens" card (create dialog with name, scope checkboxes, expiry; the token shown once with copy; revoke), user detail lists live tokens with revoke; strings under `account.token*`. Tests: `api/tests/pats.rs` (mint/list/revoke with recent-auth and scope rules, use on the account and admin APIs with confinement and role loss narrowing, no nesting, expiry and nonsense refused, admin list/revoke, deletion revoking, tenant switch-off, introspection), `ui/e2e/pats.spec.ts`; README, e2e README.
- [x] 8.6 Tests: account endpoints self-scoped only (cannot read another user), email/phone change verification, GDPR export completeness, deletion → purge job, upstream IdP brokering against a mock OIDC provider (state/nonce checks, linking policies, first-login completion), device flow polling states (pending/slow_down/denied/expired), PAT scope enforcement. Playwright e2e: account console profile, sessions revoke, linked identity. Where each lives: self-scoping in `account.rs` (`only_a_live_account_token_of_the_path_tenant_gets_in`, own devices only) and `account_self.rs` (`an_account_token_only_ever_reaches_its_own_user`: profile, sessions, consents and their writes stay with the token holder); email and phone change verification in `account_self.rs` (codes, cooldown, conflict, cancel, removal); export completeness in `account_self.rs` (every section incl. tokens and identities, the download header, no secret material) and admin export/import in `admin_tenant_config.rs`; deletion and the purge job in `account_self.rs` (`deleting_the_account_ends_everything_and_the_purge_job_removes_it_later`, plus the tenant switch-off); brokering in `broker.rs` against the mock OIDC and GitHub-like providers (state replay, nonce and issuer mismatches, denial, verified-email/explicit/always-new policies, first-login profile completion, linking from the console, admin CRUD and confinement); device flow states in `device_flow.rs` (pending, slow_down, approval, denial, cancellation, expiry, spent codes, guess limiting); PAT scope enforcement in `pats.rs` (held scopes only, narrowed on role loss, `account` scope for the account API, admin permissions for the admin API, no nesting, expiry, revocation, introspection). Playwright: `account-profile.spec.ts` (profile and sessions revoke), `broker.spec.ts` (linked identity end to end), `pats.spec.ts` (tokens). Phase 8 complete.

### Phase 9 — Scale, security, operability
- [x] 9.1 Rate limiting (per IP / client / tenant) on `/token`, `/authorize`, flow endpoints; security headers + CSP for UI; strict CORS from client config. (Fixed windows in Valkey via one Lua round trip, `settings.rate_limits` per tenant plus `RATE_LIMIT_IP_PER_MINUTE`; `RateLimit-*` and `Retry-After` headers; per-family refusal formats; per-client bucket in client auth; hardening headers + HSTS layer; hash-based CSP meta from `ui/scripts/csp.mjs`; one CORS layer with UI/own origins, public documents, cached client-origin union, and the per-client `Origin` check at client-authenticated endpoints. 9.3 admits custom-domain origins.)
- [x] 9.2 IP allow/deny rules enforcement per tenant/client. (`services/ip_rules::{cached, scope_allows, tenant_allows, require_client}`: longest prefix wins, allow rules make a scope an allow list, both scopes must pass; the tenant scope in `middleware/guard.rs` ahead of the rate limit on the token/authorize/flow families, the client scope in `client_auth::authenticate` and `authorize::finish_decision`; cache `ridm:t:{id}:ip_rules` evicted on every rule write; unreadable rules refuse.)
- [x] 9.3 Custom domains per tenant (host → tenant map, issuer override). (`middleware/host.rs`: the router's fallback maps `Host`/trusted `X-Forwarded-Host` to a tenant through `resolve_tenant_by_host` (`ridm:tenant:host:*`, unique index `tenants_custom_domain_key`) and re-dispatches the request as `/t/{slug}{path}` through the routed app; `tenants::normalize_custom_domain` validates, lower-cases and refuses the primary hosts; CORS admits the custom origin. The built-in console clients and the embedded UI on custom hosts belong to 11.1.)
- [x] 9.4 Token exchange (RFC 8693), DPoP (RFC 9449). (`token.rs::token_exchange` with `act`, scope narrowing, audiences, lifetime capped by the subject token; `oidc/dpop.rs` proof verification with jti replay guard in Valkey, `cnf.jkt` binding, `Presented`/`enforce_binding` at userinfo, admin and account middlewares, bound refresh tokens for public clients (`refresh_tokens.dpop_jkt`), `clients.dpop_bound_access_tokens` (+ DCR metadata, console toggle); discovery advertises both. Not done: server nonces, `dpop_jkt` at /authorize.)
- [x] 9.5 Webhooks delivery engine (signed payloads, retry with backoff, dead-letter) driven by the event bus. (5.10 laid the queue, signing, backoff and dead status; 9.5 adds prompt delivery from the dispatcher under a per-tenant Valkey lock, concurrent attempts through one shared client, `X-RIDM-Timestamp`, the `webhook.delivery_dead` event (never dispatched itself), bulk `redeliver-dead` with a console button, private-address target refusal, delivery metrics; the job now visits only tenants with due rows (`repos::webhooks::tenants_with_due` in a bypass transaction) instead of every tenant.)
- [x] 9.6 SCIM 2.0 server (users/groups) for tenant provisioning, with bearer auth per SCIM client. (`routes/scim.rs` + `services/scim.rs`: discovery documents, Users/Groups CRUD, RFC 7644 filter parser with index lookups for userName/externalId/emails/id and a bounded scan otherwise, PATCH applied on the SCIM document then stored as a replace, offset paging over the keyset list; `scim_tokens` (`rscim_`, hashed, expiring, revocable, `ridm:scim:*` permissions held by user managers) with admin routes and the console Provisioning page; `users.external_id` column with a unique partial index.)
- [x] 9.7 Background jobs: expired tokens/sessions/flows cleanup, audit retention, soft-delete purge, message retry, key rotation; leader lock in Redis. (Audit retention, purge, message and webhook retry and key rotation existed with the leader lock; 9.7 adds the hourly `cleanup` job — `repos/cleanup.rs` targets with batched cross-tenant deletes under `RETENTION_DAYS`, sessions a week at most —, `jobs/status.rs` last-run records plus run counters and duration histograms in the scheduler, and the message job visiting only tenants with due rows like the webhook job. Flows, codes and PAR requests live in Valkey with TTLs and need no job.)
- [x] 9.8 OpenTelemetry OTLP trace export; Prometheus metrics complete (logins, tokens, latencies, queue depths); audit export sink (syslog/HTTP). (`telemetry::init_server` adds a `tracing-opentelemetry` layer over an OTLP/HTTP protobuf batch exporter when `OTEL_EXPORTER_OTLP_ENDPOINT` is set, `telemetry::prometheus` installs the recorder once, `GET /metrics` renders it behind `METRICS_TOKEN`; `middleware/http_metrics.rs` per-route counts and latency histograms; login, token-request, session, queue-depth, job and audit metrics; `services/audit_sink.rs` HTTP batches or syslog UDP/TCP fed from the audit writer over a bounded queue.)
- [x] 9.9 Redis Sentinel/Cluster support; Postgres read-replica routing for read-heavy admin queries. (`cache/pool.rs`: `Topology::parse` of `redis://`, `redis+cluster://`, `redis+sentinel://.../master`, one `Cache` over deadpool's single, cluster and sentinel pools with a topology-neutral `CacheConn`, `pubsub_client` resolving the Sentinel master; invalidation deletes one key per DEL and the rate limiter runs one script per bucket so a cluster accepts them. `DATABASE_READ_URL` → `state.db_read` + `db::read_tx` (READ ONLY) for the list and stats services. Cluster and Sentinel themselves are not in the test infrastructure: parsing and the neutral connection are covered, live topologies are documented.)
- [x] 9.10 Perf: `EXPLAIN` review, indexes, k6 load test targeting 5k token req/s per node; JWKS/discovery cached with ETag; pool tuning. (EXPLAIN on the hot queries shows index scans, no new indexes needed; the token path now caches resource servers by identifier, permissions per role set and a user's groups under the roles version — `resource_servers::{find_by_identifier_cached, permissions_for_roles_cached}`, `groups::groups_of_user`; JWKS/discovery ETag caching existed since Phase 3; `REDIS_POOL_MAX`; `perf/token.js` k6 script with the PR `load-smoke` job (debug build thresholds) and a documented release baseline.)
- [x] 9.11 OIDC conformance suite (basic OP, config, PKCE, RP-initiated logout, back-channel logout, DCR profiles); fix findings. (`conformance/`: the OpenID Foundation suite from its released images behind its own nginx, rIDM behind a Caddy TLS front as `https://ridm.local` with a private CA the suite trusts, and `driver.mjs` — a headless Chromium on the suite's network that signs in, consents, confirms logout and uploads the screenshots the review tests wait for, because the suite's HtmlUnit cannot run rIDM's React pages; `run.sh` drives the vendored `run-test-plan.py` over the configuration, basic, RP-initiated, back-channel and front-channel logout plans, and the `conformance` workflow runs them per PR and weekly. Findings fixed: the ID token minted from a refresh token now repeats the original `auth_time`, `amr` and `acr` (carried on the token family, migration `20260917121258_refresh_token_auth_context`); replaying an authorization code now also denies the access token it produced, not just the refresh family; a session always records an `acr` (`urn:ridm:acr:single`, or the MFA class) and discovery advertises `acr_values_supported`; the scope-derived standard claims left the ID token for `/userinfo` (OIDC Core §5.4) behind the new per-client `id_token_scope_claims` opt-in (migration `20260917121729_id_token_scope_claims`, DCR metadata, console toggle); `dcr.require_pkce` lets a tenant register confidential clients without PKCE, which the basic profile needs. Accepted with reasons in `expected-failures.json` (the `tid` tenant claim) and `expected-skips.json` (the four `alg=none` tests). Tests: `api/tests/conformance.rs`.)
- [x] 9.12 Security review pass (`/security-review`), `cargo audit`/`cargo deny` in CI, threat model document. (`cargo audit` and `cargo deny` already ran per PR; the deny step passed `--all-features` after the subcommand, which cargo-deny 0.19 rejects, so the setting moved into `deny.toml` where it is version-proof, and an `npm audit --omit=dev` step was added for the UI, which had none. `THREAT_MODEL.md`: assets, trust boundaries, adversaries, threats answered with the code that answers them, verification, the assumptions a deployment must hold, and known gaps; linked from the README and SECURITY.md. The review pass found three real defects, all fixed with regression tests: the request guard read the tenant from the raw path while handlers read axum's decoded parameter, so an escaped slug reached the tenant with its IP rules and rate-limit buckets skipped; the client address came from the leftmost `X-Forwarded-For` entry, which a caller supplies when the proxy appends, making IP rules advisory; and token exchange checked neither who the subject token was issued to nor its `cnf`, so any exchange-capable client could mint a token for any audience carrying another user's identity, and could strip a DPoP binding.)
- [x] 9.13 Tests: rate-limit thresholds and headers, IP rules, custom-domain issuer resolution, token exchange + DPoP proofs (replay, wrong htm/htu), webhook signing/retry/dead-letter, SCIM filter/patch semantics, cleanup jobs with leader lock (two nodes, one runs). Conformance suite as a required PR check. k6 smoke with thresholds as a required PR check; full baseline on release. cargo-fuzz bounded run per PR, long run weekly. (Where each already lived: rate-limit thresholds and the `RateLimit-*`/`Retry-After` headers, per-family, per-client and tenant-total buckets, the deployment-wide address ceiling and the trusted-proxy rule in `rate_limits.rs`; IP rules — tenant and per-client, allow-lists by longest prefix, live changes, cross-tenant, forged forwarding headers — in `ip_rules_enforcement.rs`; custom-domain issuer resolution (the host serves the tenant without the prefix, tokens and discovery carry that issuer, changes take effect at once, CORS) in `custom_domains.rs`; token exchange in `token_exchange.rs`, DPoP replay/`htm`/`htu`/`iat`/`ath` at the token endpoint in `dpop.rs` with `exchange_cannot_strip_a_sender_constrained_binding`; SCIM filter and patch semantics in the `services/scim.rs` unit tests and `scim.rs`; cleanup retention and the leader lock in `jobs.rs`. Added here: `dpop.rs::a_resource_proof_is_good_for_one_request_at_one_url` (a proof replayed at a resource, another method, another endpoint's `htu`, a stale `iat`); `webhook_engine.rs::every_delivery_is_signed_over_its_body_and_the_secret_can_be_rotated` (the receiving end recomputes the `X-RIDM-Signature` HMAC over `"<t>.<body>"`, the event/delivery/webhook and configured headers, rotation switching the key) and `a_retryable_failure_waits_for_the_backoff_and_a_permanent_one_dies_at_once` (the 30 s/2 m/10 m/30 m/2 h/6 h ladder, the job leaving a delivery alone until it is due, dead-lettering at `max_attempts`, a 404 dying on the first attempt); `scim.rs::the_filter_grammar_and_patch_removals_work_over_http` (`or`/`not`/parentheses/`pr`/`ne`/`gt`/`le`/`co`/`ew` over the scan path, grammar refusals as `invalidFilter`, `remove` of an attribute and of one element by filter, `invalidSyntax`); `jobs.rs::two_nodes_contend_for_the_lock_and_only_one_of_them_runs` (two app instances over one Postgres and one Valkey: one winner, both yielding while it is held, the job free again after the release); the cleanup suite beside it now waits out whichever sibling test holds the lock (`cleanup_now`) and asserts on row counts rather than on how many rows its own pass deleted, since a pass on any node purges every tenant. CI: the `fuzz-smoke` job (nightly + cargo-fuzz, 60 s per target, crashing inputs uploaded) joins `load-smoke`, `conformance` and the rest as required checks, now listed in CONTRIBUTING beside §6; `weekly.yml` fuzzes each target for four hours, one job per target under the runner's six-hour ceiling, uploading corpus and artifacts; `release.yml` runs the 200-VU k6 baseline against a release build on a `v*` tag and puts the summary in the job output. `api/fuzz/` (four targets since Phase 3.10) gained the `scim_filter` target, committed seeds, `run.sh` (seeds → corpus, time budget, reproduce hint) and a README, exact-pinned dependencies, and invariants beyond "does not panic": no input verifies against a key that did not sign it, a non-native redirect matches only byte-identically, a verifier verifies against its own challenge and no other. The first CI run of the phase found three faults of its own, each fixed on the branch: `cargo fuzz` builds for the triple the tool was built for, so a binstalled (musl) cargo-fuzz asked for a musl std nobody had — `run.sh` now passes the toolchain's host triple; `load-smoke` opened dynamic registration with `jsonb_set`, a no-op on an absent `dcr` key, so k6 could not register its client; and the conformance suite caught rIDM signing a token with a key its own JWKS never published — concurrent first contact on a fresh tenant had every request path generating its own key (`get_or_load` runs its loader on every concurrent miss, and nothing constrained a tenant to one active key per algorithm), after which tokens used the newest while a cached document named an older one. Fixed by migration `20260917180540_one_active_signing_key_per_alg` (surplus active keys retired, then a partial unique index that makes `ensure_active`'s existing conflict arm real), a per-tenant creation lock so one node generates the first key while the others wait for it, and a JWKS cache keyed by a per-tenant keys version (the roles-version pattern) so a document read before a key write cannot be stored after it; `api/tests/signing_keys.rs::one_key_is_made_for_concurrent_first_contact_and_it_is_the_published_one`. Phase 9 complete.)

### Phase 10 — Developer experience and CLI
- [x] 10.1 `ridm` CLI: bootstrap, tenant export/import/diff, key rotate, master-key rotate, user create/reset, client create; talks to admin API (or DB for bootstrap). (`crates/ridm-cli` → binary `ridm`, clap 4.6.7 derive. `cli.rs` holds every flag; `context.rs` resolves profile, server URL, tenant and bearer once per command; `api.rs` sends the request and turns a refusal back into its RFC 9457 document (`error.rs::ApiError`, boxed so no `Result` carries its size); `output.rs` prints aligned tables, or the response verbatim with `--output json`; `input.rs` keeps passwords, secrets and documents off the command line. Commands in `commands/`: `login`/`logout`/`whoami`/`profile list|use|show`, `tenant list|show|create|export|import|diff`, `key list|rotate`, `master-key status|rotate`, `user create|reset`, `client create`, `bootstrap`. `tenant diff` is `POST …/import?dry_run=true` rendered as `+`/`~`/`-` with per-field `from → to`, and `--exit-code` returns 3 when the plan is not empty; `tenant import` runs that same request first, shows the plan, asks, then applies and prints once the secrets the server minted; `user reset` takes an id, a username or an email and refuses anything that is not exactly one account. `auth.rs` gets the bearer three ways — a personal access token (what `ridm login` asks for by default), client credentials of a machine client, or the device grant (RFC 8628 with the `slow_down` ladder) — always asking for `resource=urn:ridm:admin`, and refreshes an expiring OAuth credential in place before the request that needs it. Profiles live in `~/.config/ridm/config.json` (`$RIDM_CONFIG`/`$XDG_CONFIG_HOME` honoured) written `0600`; `--url`/`--tenant`/`--token` (`RIDM_URL`/`RIDM_TENANT`/`RIDM_TOKEN`) override without writing anything, so a pipeline stores no file. Exit codes: 0 done, 1 the command ran and failed, 2 the command line or the configuration was wrong, 3 `diff` found changes; a command that rewrites or destroys with no terminal to ask refuses rather than assuming yes. `bootstrap` is the one command that cannot use the admin API — no token can exist yet — so it links `ridm-api` behind the default `bootstrap` feature and calls the same `services::bootstrap::run` the server calls at startup; `--no-default-features` builds a slim, HTTP-only `ridm`. `clap` added to §2 and the workspace, `ridm-api` given an explicit version so `cargo deny` sees no wildcard. 19 unit tests (`--bins`) cover problem-document parsing, the profile file format and selection, plan emptiness, value truncation, query building and the import error path; the whole surface was exercised against the dev server. Deliberately not here: a built-in device-grant client for the CLI (the console clients do not allow that grant, so `--device` needs a client an operator registers), and the CLI integration tests against the test harness, which are 10.6.)
- [x] 10.2 `ridm-auth` crate: axum extractor/middleware that validates rIDM JWTs (JWKS cache, audience/permission checks). Published to crates.io. (`crates/ridm-auth`, the relying-party half: `Validator::builder(issuer).audience(..).discover()` reads `jwks_uri` from the discovery document — refusing a document that names a different issuer — and `validate(token)` checks the signature, `typ` = `at+jwt`, an asymmetric `alg` that matches what the key was published for, `iss`, `aud`, `exp`/`nbf` with 60 s of leeway, and whatever scopes, permissions and roles were asked for. `claims.rs` is the typed `Claims` — `aud` read from a string or a list, `roles`/`groups`/`permissions`/`amr` from either spelling and absent rather than fatal when a tenant maps them to something else, mapped claims kept in `extra` — with `has_`/`require_` for scopes, permissions, roles and groups. `jwks.rs` caches the key set under two clocks: `jwks_max_age` (10 min) is how long a cached set answers at all, so a revoked key stops verifying within that window, and `min_refresh_interval` (30 s) is the floor between fetches, so tokens naming keys that do not exist cannot make an API into a load generator against the issuer; concurrent misses share one fetch through a `tokio::sync::Mutex`, and while the issuer is unreachable the last good set keeps answering. `error.rs` separates 401 `invalid_token` from 403 `insufficient_scope` from a 503 that is worth retrying, each with an RFC 6750 `WWW-Authenticate` challenge whose description cannot break out of its quoted string. `axum.rs` (default feature): `Guard::new(v).permission(..).realm(..)` as a `from_fn_with_state` route layer, `RidmClaims`/`OptionalRidmClaims` extractors that reuse what the guard validated, `IntoResponse for AuthError`. Two refusals are deliberate and documented: a sender-constrained token (`cnf.jkt`) is refused rather than downgraded to a bearer token unless `allow_sender_constrained(true)`, since no DPoP proof is verified here, and an `http://` issuer needs `allow_http(true)`. No revocation check — access tokens are short-lived and `/introspect` is the answer for an API that must react sooner. Unlike the rest of the workspace the manifest asks for compatible ranges, not exact pins: exact pins in a published library would conflict with its dependents, and the workspace lockfile still pins what CI builds. There is no `native-tls` feature — `deny.toml` bans that crate — and a caller who needs another TLS stack passes their own client to `http_client`. Tests: 51 in the crate — the unit tests beside each module, `tests/validation.rs` (a stand-in issuer over real HTTP: acceptance and claim reading, discovery issuer mismatch, one fetch then cache, refresh on an unknown kid after rotation, the cooldown holding a miss and lapsing, 16 concurrent misses making one request, a stale set answering while the key set is 503, a forged signature under a published kid, a key published for another algorithm, a missing kid, an HS256 token, an ID token refused by `typ` and `application/AT+JWT` accepted, wrong audience and wrong issuer, expiry and `nbf` inside and outside the leeway, `cnf` with and without the opt-in, builder requirements, machine tokens, the `Authorization` header, and garbage in place of a token) and `tests/axum_integration.rs` (the guard serving, challenging and forbidding; the extractor and the guard refusing alike; the optional extractor; the handler reusing the guard's validation; a non-UTF-8 header). `api/tests/ridm_auth.rs` runs the published crate against the real server: a machine client with a service account holding `orders:read` takes a `client_credentials` token over HTTP for one resource server, the crate discovers the tenant, verifies it and reads the permission; a token for the other resource server is refused; and `keys::rotate` is picked up on the next request while the retiring key still verifies what it signed. CI: `cargo package -p ridm-auth --locked` in the `static` job, so the crate has to keep standing up outside the workspace. Not done here: `cargo publish` itself, which belongs with the version bump at release (11.4/11.6) — `0.1.0-dev` is not a version to hand anyone.)
- [x] 10.3 Example RP apps: Next.js SPA (PKCE), Rust axum API using `ridm-auth`, generic confidential client. (`examples/`: three relying parties against one demo tenant, each covering a different half of the protocol. **`axum-api`** (`ridm-example-axum-api`, ~200 lines) is the resource server: one `Validator` warmed at startup, a `Guard` per route subtree (`orders:read` on `GET /orders`, `orders:write` on `POST /orders`), `/whoami` behind the extractor alone to show the no-permission tier, CORS for the two browser origins, and orders in a `Mutex`. **`nextjs-spa`** (Next 16 static export, the versions §2 pins) is the public client: discovery, PKCE over WebCrypto, `state`/`nonce`, RFC 8707 `resource`, tokens in a module variable rather than `localStorage`, and a `prompt=none` top-level redirect on load so a reload picks the SSO session back up — with a `sessionStorage` flag so a signed-out user does not bounce; the ID token's signature is not re-verified in the browser, which OIDC Core §3.1.3.7 allows for a token taken straight from the token endpoint over TLS, while `iss`, `aud`, `exp` and the nonce are all checked. **`confidential-client`** (`ridm-example-confidential-client`) is the server-side web app: the code comes back to it, it spends it with `client_secret_basic`, and the browser gets only a session cookie (`HttpOnly`, `SameSite=Lax`, `Secure` on https); `config.rs`/`oidc.rs`/`session.rs`/`pages.rs` split the environment, the protocol, the stores and the HTML; `/callback` refuses an unknown or spent `state`, a stale pending request, an ID token that does not verify and a nonce that does not match; the access token is refreshed in place with rotation; `/logout` revokes the refresh token then sends the browser to `end_session_endpoint`; and `/backchannel-logout` verifies a `logout+jwt` and ends the session the `sid` names. That last one is the clearest demonstration of what `ridm-auth` is for: two validators over the same keys differing only in `token_type` (`JWT` vs `logout+jwt`) and audience, which is what stops one kind of token being spent as another. `examples/demo-tenant.json` is an ordinary tenant configuration document (resource server + two permissions, two scopes, `orders-reader`/`orders-manager`, the SPA and web clients with their redirect, CORS and back-channel URIs) and `examples/setup.sh` applies it with `ridm tenant import`, creates `dana` and `sam` and gives them one role each — re-runnable, and it goes to the admin API directly for exactly one step, role assignment, which the CLI has no command for. Verified against the live dev server end to end: the document imports (and re-imports as a no-op), the API example discovers the tenant and warms its key set, an unauthenticated call gets the RFC 6750 challenge, a `client_credentials` token minted for `https://orders.example` reads `/whoami` and places an order, the same client with only `orders-reader` gets `403 insufficient_scope` naming `orders:write`, the confidential client builds both validators and an authorization request rIDM accepts into a login flow, and its back-channel endpoint refuses both nonsense and an `at+jwt`. That run found one thing worth changing in `ridm-auth`: `Claims::is_client_credentials` was renamed `is_client_only`, because a machine client *with* a service account carries that user as `sub` and should read like any other user — the useful question is whether anyone is behind the token at all. The examples are workspace members so `cargo check`/`clippy` cover them, which needed the `api/Dockerfile` stub stage and `.dockerignore` updating (the Node example stays out of the build context), and the path dependency carries an explicit version so `cargo deny` sees no wildcard. Not done here: the CI smoke tests against docker-compose and a browser sign-in, which are 10.6.)
- [x] 10.4 Docs site (mdBook or Docusaurus): concepts, quickstarts, admin guide, API reference (OpenAPI), deployment guide, migration guide from Keycloak/Auth0 (import mapping). (mdBook 0.5.4 in `docs/` — chosen over Docusaurus to keep a third npm project out of the tree. 54 pages in six parts: **Concepts** (tenants and issuers, clients, users/groups/roles, resource servers/scopes/permissions, tokens, keys and the master key, flows and sessions, MFA and passkeys, brokering, config as code, events/audit/webhooks), **Quickstarts** (run locally, protect a Rust API with `ridm-auth`, SPA with PKCE, server-side web app, machine-to-machine), **Admin guide** (consoles, the `ridm` CLI, tenants and settings, users and bulk import, clients, administrator access, MFA policy, messaging, SCIM, webhooks and audit, key rotation, custom domains, rate limits/IP rules/CAPTCHA), **Reference** (every non-admin endpoint, the admin API, token claims, every environment variable, the tenant document, errors), **Deployment** (overview, docker-compose, container image, TLS and proxies, Postgres and Valkey, scaling, observability, a production checklist) and **Migration** (strategy, Keycloak, Auth0). The admin API reference is Redoc 2.5.4 (pinned, SRI) rendering the committed `api/openapi.json`, which `docs/build.sh` copies in rather than duplicating. The migration guides carry `jq` scripts that turn a Keycloak realm export and an Auth0 user/hash export into rIDM's bulk-import and tenant documents; their password-hash conversions were tested against hashes made the way Keycloak (JDK PBKDF2, BouncyCastle Argon2) and Auth0 (bcrypt) make them, parsed and verified by rIDM's own `bulk_users`/`password` code. Pages were written from the code rather than the README, and every claim checked against it. CI: a `docs` workflow builds the book on every PR, fails on a page left as a placeholder, checks every internal link and anchor offline with lychee, and on `main` deploys to GitHub Pages (`https://zerosandonesllc.github.io/rIDM/`; Pages must be enabled with source "GitHub Actions" for the deploy job to succeed). **Writing the docs against the code found bugs, fixed here**, each security fix with a regression test in `api/tests/security/` (now 23): `__Host-` cookies were set with `Path=/t/{slug}`, which browsers reject, so no browser session survived with `COOKIE_SECURE=true` (now `Path=/` and per-tenant names, `__Host-ridm_session_{slug}`); `/authorize` honoured a session whose MFA or forced password change was abandoned (the tenant's policy is now re-checked on every live session, and at device approval); bulk user import, tenant import and SCIM group membership granted admin permissions without the no-escalation check; revoking one session left its refresh tokens working and only RP-initiated logout sent back-channel logout (every sign-out path now does both); a tenant's custom domain answered `/admin`, `/metrics` and other tenants' paths; webhook and other tenant-chosen outbound URLs were checked only as IP literals (now a public-address-only DNS resolver, no redirects, tenant SMTP included); refresh `resource` could widen the audience and an ungranted `scope` spent the token before refusing; the app role could not create audit partitions (now a `SECURITY DEFINER` function); OTLP exported nothing at the default log level; `--healthcheck` failed under native TLS; and a set of stored-but-inert settings now act — opaque access tokens (`at_…`, Valkey-held, introspection-only), per-resource-server `signing_alg`, `offline_access`/`allow_offline_access` (refresh tokens without it now end with the session), scope `claims`/`is_default`/resource-server binding, profile `visible_in`, `BOOTSTRAP_SAMPLE_CLIENT`, DCR initial access tokens (admin routes, `ridm client iat`, a console block). Smaller: claim-mapper `roles`/`user_attribute` fixes and protected `cnf`/`act`/`permissions`, discovery's DPoP algorithms, the dead CAPTCHA toggle, template preview variables, MFA-mode PATCH, admin user-create statuses, recovery codes on last-factor removal, `editable_by: none` via imports, argon2 variant labels. Three migrations. README, GETTING-STARTED, THREAT_MODEL, CONTRIBUTING and the examples corrected where they disagreed with the code. Left as documented limits: a custom domain still cannot hold the browser session the sign-in pages set on the API host (with the embedded UI, 11.1), and `ridm bootstrap` migrates by default while `ridm-api bootstrap` no longer does.)
- [x] 10.5 Dev mode polish: seeded data, Mailpit, hot reload notes, `make`/`just` targets. (**`Makefile`** — make rather than just, because every machine already has it; `make` alone prints the targets grouped by section. `up`/`down`/`reset` (asks before `down -v`)/`ps` drive Postgres, Valkey and Mailpit through the compose file with `--env-file .env`; `migrate` runs `ridm-api migrate` as the schema owner (no `sqlx-cli` needed); `setup` = up + migrate + token; `api`, `watch` (watchexec restarting on Rust/SQL/TOML under `api/` and `crates/`), `ui` (`next dev` on 3110 with `API_PROXY`), `mail`; `token`, `seed`; `fmt`, `lint`, `test`, `test-all`, `e2e`, `openapi` (written through a temp file), `docs`. Ports and URLs come from `.env` with defaults, all overridable on the command line. **An admin token without a browser**: `ridm bootstrap --issue-token NAME [--token-days N]` creates the owner if there is none, then mints a personal access token for `--username` carrying every admin permission that user holds, through `personal_access_tokens::create` (so the tenant's PAT policy and expiry cap apply), and prints it alone on stdout while everything else goes to stderr; on a bootstrapped database it asks for no email or password. `make token` writes it to `target/dev/token` (0600). **Seeded data**: `dev/seed.sh` (`make seed`) runs `examples/setup.sh` for `demo`, then creates `acme` from `dev/acme-tenant.json` — a profile schema (enum department, patterned employee ID, date, a user-editable nickname), a nine-group tree under `staff` whose groups carry roles (`viewer`, `editor` ⊃ viewer, `billing-admin` ⊃ viewer), a resource server with four permissions and one client of each type (web with consent, native, device, machine with a service account) — and bulk-imports `$ACME_USERS` (120) generated users with attributes, locales and group memberships, a few disabled or unverified, five with the demo password; re-runs report existing users rather than failing. **Faster debug builds where it matters**: `[profile.dev.package.*]` compiles `rsa`, `num-bigint-dig`, `num-integer`, `num-traits`, `argon2` and `blake2` at `opt-level = 3` — RSA key generation in the key test went from 8.15 s to 0.59 s and the argon2 test from 0.28 s to 0.02 s, which is what tenant creation and every sign-in wait for. **Found on the way**: one-shot commands never wrote their events to the audit log — the audit writer is a background task only the server spawns, so `ridm bootstrap`, `ridm-api bootstrap` and `ridm-api rotate-master-key` published into a bus nobody read, and an owner token minted from the CLI would have left no trace. `services::audit::CommandRecorder` subscribes before the command acts and records what was published before it exits; test `admin_audit::a_command_records_what_it_published_before_it_exits`. Mailpit was already in the compose `dev` profile; the docs now say how the API reaches it. Hot reload notes in README (Development), GETTING-STARTED ("The short way", "Hot reload") and the docs site's local quickstart; `--issue-token` in the CLI guide and README; CONTRIBUTING and `examples/README.md` point at the targets.)
- [x] 10.6 Tests: `ridm` CLI integration tests against the harness; ~~`ridm-auth` crate tests (JWKS cache refresh on unknown kid, audience/permission checks, clock skew)~~ (done in 10.2: `crates/ridm-auth/tests/` and `api/tests/ridm_auth.rs`); example apps smoke-tested in CI against docker-compose. (Where each lives: **`crates/ridm-cli/tests/cli.rs`** runs the built binary (`CARGO_BIN_EXE_ridm`) as a separate process against the server's own harness, `api/tests/common` included by `#[path]` (the crate gained the harness's dev-dependencies), each run in its own working directory with its own `RIDM_CONFIG` and an emptied environment so no developer `.env` or `RIDM_*` leaks in, and awaited with `tokio::process` because the in-process server shares the test's runtime. Ten tests: `login --token-stdin` writes a 0600 profile that `profile use`/`whoami`/`tenant list` then use, `profile show` redacts it, `logout` forgets the credential and `--all` the profile; a refused credential (garbage, or a real token for a user without admin roles) exits 1 and is never stored; usage mistakes exit 2 before any request (no URL, no credential, unknown command, a document that is not JSON, an empty stdin) while a missing file exits 1; `tenant export` to a file and to stdout, `diff --exit-code` 0 on the export then 3 with the added role in the plan, `import` on a pipe without `--yes` refusing and applying nothing, `import -f - --yes` from stdin, a re-import applying nothing, and no profile file written by an environment-only run; a tenant-scoped owner refused another tenant's export and keys, tenant creation and the master key; `user create --password-stdin`, a duplicate refused with 409, `user reset` by email with `--no-must-change` and by name with a generated password, each verified through `password::verify_and_upgrade`, and a user who is not there refused; `client create` for a machine client whose printed secret is spent at `/token`, a web client printing its secret, a SPA printing none, and an IAT created, listed without its secret and revoked; `key rotate` seen in `key list` (old key `retiring`, one new `active`, `--status` filtering); a global owner creating a tenant, `master` as the default tenant, a taken slug refused, `master-key status` and `rotate` refusing to ask on a pipe; and `bootstrap --issue-token` (feature `bootstrap`) printing nothing but an `rpat_` token on stdout that `whoami` accepts, exiting 2 for an unknown `--username` without printing a token and for a missing configuration. They run in the `integration` job's `cargo test --workspace --tests`. **Examples smoke**: `examples/smoke/run.sh` starts `deploy/docker-compose.yml` (profile `prod`) from a built image with `examples/smoke/compose.yml` on top — the API on the host network, because rIDM calls the web app's back-channel endpoint at the `localhost:3200` the demo tenant registers, which on the default bridge is the container itself — then bootstraps the first admin and a token with `ridm bootstrap --issue-token` as the DML-only role, runs `examples/setup.sh` (reading the one-time `orders-web` secret from its output and keeping it out of the log), runs it again and checks `tenant diff --exit-code` is clean, starts the orders API, the web app, the SPA's static export and rIDM's own UI as a static export hosted apart from the API (`NEXT_PUBLIC_API_URL`, credentialed CORS — the separately-hosted layout), and drives headless Chromium through `examples/smoke/smoke.mjs` (Playwright loaded from `ui/`'s pinned install, so no new npm project): the orders API refuses an anonymous call with a `Bearer` challenge; `dana` signs in to the web app with her password and places an order; the SPA in the same browser signs her in with `prompt=none` without a login form ever loading, sees that order and places another; signing out of the SPA ends the rIDM session and back-channel logout ends the web app's session; `sam` sees the orders in both apps, has no order form, and his order posted anyway is refused 403 by the API. Every port is an environment variable so it runs beside a development stack; logs and failure screenshots go to `target/smoke/` (uploaded by CI on failure). CI: a new `examples-smoke` job in `ci.yml` (image from the GHA build cache, Rust, Node, Chromium), added to the required-check lists in §6, CONTRIBUTING and README. `setup.sh` now takes `SPA_URL`/`WEB_URL` and substitutes them into `demo-tenant.json` as it imports, which the smoke needs for its ports and which replaces GETTING-STARTED's hand `PATCH` for a taken port 3100 (it was undone by every re-run). Verified locally end to end — the web app logs `back-channel logout ended=1` — and the failure path checked: a wrong expectation exits 1 with a screenshot and leaves no containers or listeners behind. Not done here: adding `examples-smoke` to the `RequirePR` ruleset, which needs the repository owner.)

### Phase 11 — Packaging and release
- [x] 11.1 Embedded UI mode: `rust-embed` serves `ui/out` with trailing-slash fallback; image build runs `npm run build`. (Built as: cargo feature **`embedded-ui`** on `ridm-api` (off by default so a Rust-only checkout builds without Node; `allow_missing`, so `--all-features` jobs compile without `ui/out` and the server just logs that it has no UI) compiling `ui/out` in with rust-embed 8.12 (`mime-guess`; a debug build reads the files from disk). **`routes/ui.rs`**: `EmbeddedUi` (a lookup fn, so tests inject a fixture export) held in `AppState.ui`, set by `EmbeddedUi::from_build` when the feature is in, `EMBEDDED_UI` (new, default true) is on and `UI_URL` is `PUBLIC_URL`'s origin at `/`; served as the router's `fallback_service` behind tower-http gzip, so API routes always win and a miss under an API prefix (`t`, `admin`, `scim`, `.well-known`, probes, `metrics`, `docs`, `openapi.json`) stays a bare 404. Resolution: `dir/` → `dir/index.html`, exact files, `dir` → `308 dir/` with the query kept, else `404.html` with 404; only plain relative paths (no `.`/`..`/empty segments, backslashes or `%`). Headers: weak `ETag` from the file's SHA-256 with `If-None-Match` → 304, `/_next/static/` immutable for a year and everything else `no-cache`, `charset=utf-8` on text, and framing: `DENY`/`frame-ancestors 'none'` on every page but `/login/`, which gets `SAMEORIGIN`/`'self'`. **Custom domains** (`middleware/host.rs`): on a node serving the UI, a GET/HEAD of an exact file of the export passes unprefixed (not `/console/`, not `/`; a page without its slash stays the tenant's route, e.g. `/account/me`), and the new `AppState::ui_page(tenant, …)` (plus `tenant_ui_base`, `config::page_url`) sends every page and emailed link for a custom-domain tenant to `https://{domain}/…` — `/authorize` on either host, end-session, device, broker, recovery, invitation, verification, magic link — so the session the pages set belongs to the host `/authorize` answers on and passkeys (RP id = the domain) work; `account_console::desired(state, tenant)` adds the host's `/account/callback/` and `/account/` to the built-in account client, re-ensured on tenant create and whenever `custom_domain` changes (`for_every_tenant` now hands over the tenant row). **Image**: `api/Dockerfile` gains a `ui` stage (`node:24.21.0-bookworm-slim` on `$BUILDPLATFORM`, `npm ci` + `npm run build` with `NEXT_PUBLIC_API_URL` empty) and builds with `--features embedded-ui`; `.dockerignore` drops UI test output. **CI**: `ui-e2e` builds the same-origin export and the API with the feature and runs Playwright with `E2E_UI_URL` = the API (§6's "e2e covers the artifact that ships"). That run found a real bug: the console's branding preview frames `/login/`, but no exported page allowed `frame-src 'self'` and every static-host recipe sent `frame-ancestors 'none'`, so the preview was broken on any static deployment (`next dev` hid it); `ui/scripts/csp.mjs` now gives every console page `frame-src 'self'` (every one, since the console navigates client-side and the first page's meta policy stays in force) and the server lets only `/login/` be framed by its own origin. Tests: `api/tests/embedded_ui.rs` (13, over `tests/fixtures/ui`: directory indexes, the slash redirect with query, the 404 page, API prefixes and methods keeping their 404, ETag/304 with weak comparison, immutable bundles, gzip, HEAD, encoded traversal, no UI → bare 404, custom-host pages without the console or root, `/authorize` on either host sending the browser to the custom host, pages staying at `UI_URL` without the embedded UI, and the account client following the domain), unit tests for path safety and API prefixes, `csp.test.mjs` for the console framing rule; the full Playwright suite (103) passed against an embedded-UI build, and the image was booted and checked. Docs: deployment overview ("Where the UI is served from"), container stages, nginx/Caddy recipes rewritten for the one-upstream layout with a separately-hosted variant carrying the `/login/` exception, configuration reference (`EMBEDDED_UI`), security headers, custom domains, consoles, local and compose quickstarts, README, GETTING-STARTED, THREAT_MODEL, `.env.example`, `ui/e2e/README.md`.)
- [x] 11.2 Helm chart (`deploy/helm/ridm`): Deployment, Service, Ingress, HPA, external Postgres/Redis values, existing-Secret refs, PodDisruptionBudget. (Built as: chart `ridm` 0.1.0-dev (apiVersion v2, Kubernetes ≥1.27) with templates for the **Deployment** (non-root 65532, read-only root filesystem, all capabilities dropped, `RuntimeDefault` seccomp, no service-account token, `/tmp` emptyDir, startup/liveness probes on `/healthz` and readiness on `/readyz`, rolling updates with `maxUnavailable: 0`, checksum annotations so config and secret changes roll the pods, soft topology spread over nodes and zones, 30 s grace above the 20 s drain), **Service**, **Ingress**, **HPA** (autoscaling/v2, CPU plus optional memory), **PodDisruptionBudget** (only with more than one replica), **ServiceAccount**, a **ConfigMap** of the plain environment, a chart **Secret** only for values given inline, an optional **ServiceMonitor** (bearer token from the metrics Secret) and **migrations** as a `pre-install`/`pre-upgrade` hook Job running `ridm-api migrate` as the schema owner, whose credential lives in a hook Secret deleted with the Job on success, so pods only ever hold the DML-only role. External Postgres/Valkey only. Every secret (`DATABASE_URL`, `DATABASE_READ_URL`, `REDIS_URL`, `MASTER_KEY`, `MASTER_KEY_PREVIOUS`, `SMTP_PASSWORD`, `METRICS_TOKEN`, `BOOTSTRAP_ADMIN_PASSWORD`, the migrator URL) is inline or an `existingSecret` name/key ref, the ref winning (`ridm.secretItems`/`ridm.secretEnv` helpers). The master key is mounted as a file for `MASTER_KEY_FILE` by default (`masterKey.asFile`, mode 0440 via fsGroup), out of the environment. `ridm.validate` fails the render without `publicUrl`, database, cache, master key or (with migrations on) a migrator URL; `values.schema.json` rejects unknown and mistyped values; NOTES print the issuer and console URLs, a `trustedProxies` warning and the `kubectl exec … /ridm-api bootstrap` line. `deploy/helm/lint.sh` lints `ci/inline-values.yaml` and `ci/full-values.yaml` strictly and validates the renders with kubeconform (now in CI's `static` job, helm 4.3.0 + kubeconform 0.8.0). `deploy/helm/smoke/run.sh` (kind cluster with its own kubeconfig, Postgres with `deploy/postgres/init-app-role.sh`, Valkey, the chart from a local image with two replicas and bootstrap on) checks readiness, discovery, JWKS, the embedded pages, zero restarts, the hook Job and Secret gone and the admin created, then upgrades and uninstalls; wiring it into CI as a required check is 11.7. The smoke found three things, fixed here: the migration Job inherited `BOOTSTRAP_ADMIN_EMAIL` without its password and failed configuration parsing (the Job now skips `BOOTSTRAP_*`); and **replicas starting together raced on the one-time start-up work** — both found no administrator, both created one, and the loser crashed (same for the built-in clients) — so `services/startup.rs` runs bootstrap and the console-client checks under a transaction-scoped Postgres advisory lock (`startup::prepare`/`serialized`), with a multi-threaded regression test in `api/tests/bootstrap.rs` that fails intermittently without the lock. Docs: new `deploy/kubernetes.md` page, chart README, container/overview/introduction pages and README updated.)
- [x] 11.3 Reverse-proxy examples (nginx, Caddy, Traefik); production docker-compose profile. (Built as: **`deploy/proxy/`** — `caddy/Caddyfile` (ACME by default or files via `RIDM_TLS_MODE`, one site for `RIDM_DOMAIN` plus `RIDM_CUSTOM_DOMAINS`, `/metrics` 404, `/readyz` health checks), `nginx/templates/default.conf.template` (the official image's envsubst template filtered to `^RIDM_`: http→https for known hosts and `444` otherwise, `ssl_reject_handshake` for unknown TLS hosts, upstream re-resolved through Docker DNS with `resolve`, `client_max_body_size 32m` on `/admin/`, `$proxy_add_x_forwarded_for`, `/metrics` 404) and `traefik/dynamic/ridm.yml` (a Go-templated file-provider config: one router over every host, a `/metrics` router behind an `ipAllowList` nobody matches, `/readyz` health checks, files or the `letsencrypt` resolver; static config is the service's `command:` because Traefik takes one source only). None adds security headers — rIDM sends its own and HSTS — and none imposes a body limit under the 32 MiB bulk import. **`deploy/production/`** — `compose.yml` (a file of its own rather than a profile in the evaluation compose, which publishes the backends and defaults every password): `postgres`, `valkey` (password fed to `valkey-server -` on stdin so it is not in the process list, `noeviction`), one-shot `migrate` as the schema owner, `ridm` (read-only root, all capabilities dropped, 16 MB `/tmp`, memory limit, 30 s stop grace), and one proxy by profile (`caddy`, `nginx`, `traefik`, each with a healthcheck so `--wait` means listening). Networks: `backend` is `internal` (no route out, nothing published) and `edge` has a fixed subnet so the proxy's pinned address (`RIDM_PROXY_ADDRESS`, 172.30.53.10) is exactly `TRUSTED_PROXIES` — trusting the subnet would trust the gateway Docker's userland proxy connects from, and behind nginx a forged `X-Forwarded-For` would then be recorded (checked by hand: the smoke fails that way, and fails with 413 when nginx's 32 MiB allowance is removed). Every secret is a compose secret file read through a `*_FILE` variable; the server never receives the migrator's URL. `init.sh` writes `secrets/` (0700 dir, 0644 files because compose bind-mounts them unchanged for non-root readers; never overwrites a file with content), `.env.example`, `.gitignore`. **Server**: `config.rs` gains `secret!`/`required_secret!` — `DATABASE_URL`, `DATABASE_READ_URL`, `REDIS_URL`, `SMTP_PASSWORD`, `METRICS_TOKEN`, `AUDIT_SINK_TOKEN` and `BOOTSTRAP_ADMIN_PASSWORD` each accept `*_FILE` (the variable wins, one trailing line ending dropped, an empty file is unset like an empty variable, an unreadable file is a configuration error), with a unit test; `deploy/postgres/init-app-role.sh` reads `RIDM_*_PASSWORD_FILE`. **Tests**: `deploy/production/smoke/run.sh` (new step in CI's required `packaging` job, logs uploaded on failure) makes a private CA, runs `init.sh`, boots the stack in files mode and, behind each proxy, checks over https the http redirect, the discovery issuer and JWKS, `/console/` and `/login/` framing headers with exactly one HSTS header, `/metrics` refused, the client address rIDM records (its `ridm:rl:ip:*` counters in Valkey) being neither a forged `X-Forwarded-For` nor the proxy, a 20 MiB bulk-import body read whole by rIDM (a personal access token minted in SQL makes rIDM consume it and answer 400 for the JSON; CI's first run showed Caddy and Traefik answering 502 to an unauthenticated upload that rIDM refused before reading, which a looser check had let through), and an unknown host getting nothing; then gives `master` a custom domain (a tenant with one takes it as its issuer on every host, so this is a second pass after a flush and restart) and checks it behind each proxy; once, that Postgres and Valkey publish no port, no secret value appears in `docker inspect` and the server holds no migrator URL. All passed for the three proxies against an image of this branch. Docs: new `deploy/production-compose.md`, `tls-and-proxies.md` rewritten around the shipped files (Traefik and Docker-label variant added, the separately-hosted `ui/out` variant kept and marked untested), `*_FILE` in the configuration reference and `.env.example`, docker-compose/overview/checklist pages and README pointed at the production stack.)
- [ ] 11.4 Release workflow: tag → multi-arch image to GHCR (cosign-signed, SBOM attached), static musl binaries (linux amd64/arm64), Helm chart package, changelog.
- [ ] 11.5 Backup/restore guide (DB + master key), upgrade guide, `security.txt`.
- [ ] 11.6 Cut v0.1.0: bump Cargo.toml + package.json, changelog, README final.
- [ ] 11.7 Tests: embedded-UI mode serves every route with trailing slash; Helm chart lint + kind smoke deploy as a required PR check; image starts, migrates, and passes `/readyz`; upgrade test from previous release tag.

### Phase 12 — Post-v1: Organizations and adaptive auth
- [ ] 12.1 Organizations within a tenant: CRUD, members, org-scoped roles, invitations, verified domains with auto-join, org picker in login flow, `org_id` claim.
- [ ] 12.2 Org-scoped admin roles and admin UI (org admins manage their own members).
- [ ] 12.3 Risk-based adaptive auth: signals (new device, new country, impossible travel, velocity), per-tenant policy raising MFA requirement or blocking; risk events in audit.
- [ ] 12.4 User impersonation by admins: explicit permission, banner in session, `act` claim, full audit.
- [ ] 12.5 Audit hash-chain verification tool and export sinks hardened; feature flags UI.
- [ ] 12.6 CIBA (backchannel authentication) and FAPI 2.0 security profile (PAR + DPoP/mTLS + JARM already present).
- [ ] 12.7 Tests: org isolation (org admin cannot escape org), domain auto-join, risk policy matrix, impersonation audit trail and `act` claim, CIBA flow states.

### Phase 13 — Post-v1: Enterprise federation and key custody
- [ ] 13.1 SAML 2.0 identity provider (rIDM as IdP): metadata, SP registration, SSO/SLO, attribute statements, signing/encryption.
- [ ] 13.2 SAML 2.0 upstream (rIDM as SP) as an `identity_providers` type.
- [ ] 13.3 LDAP / Active Directory upstream: bind auth, user/group sync job, mappers, write-back optional.
- [ ] 13.4 Kerberos/SPNEGO desktop SSO (optional feature flag).
- [ ] 13.5 mTLS client authentication (RFC 8705) and certificate-bound tokens.
- [ ] 13.6 `KeyEncryptor` backends: PKCS#11/HSM and cloud KMS as optional cargo features (kept out of the default build to stay provider-neutral).
- [ ] 13.7 Data residency: per-tenant database routing for regulated customers.
- [ ] 13.8 Tests: SAML IdP against a reference SP (SSO/SLO, signature validation, replay), SAML/LDAP upstream against containers (samltest-compatible SP, OpenLDAP), mTLS client auth, HSM backend against SoftHSM.

## 6. Testing strategy

Tests are a deliverable of every sub-phase, not a phase of their own. Each phase ends with a test bullet.

**Merge policy: `main` is protected. Every suite below is a required status check on every PR; a PR cannot merge until all of them pass. No admin bypass.** Suites that are too slow for full depth on every PR run a bounded version on the PR (still required) and the full version on a schedule; both are listed.

| Layer | Tooling | Where | Runs |
|-------|---------|-------|------|
| Unit | `cargo test`, mock provider traits from `ridm-core::test_support` | every crate | every PR (required) |
| Integration (HTTP) | testcontainers (Postgres 18, Redis 8), real axum router, shared seeded fixture; the `ridm` binary run against it | `api/tests/`, `crates/*/tests/` | every PR (required) |
| Tenant isolation | every admin/account/OIDC endpoint invoked cross-tenant; direct RLS bypass attempts | `api/tests/isolation/` | every PR (required) |
| Security suite | named negative cases (see phase bullets); grows with every finding | `api/tests/security/` | every PR (required) |
| Contract | discovery document vs registered routes; generated TS client vs live OpenAPI; SCIM schema | `api/tests/contract/`, `ui/` | every PR (required) |
| Migration | apply all migrations to seeded snapshot; constraints, indexes, row counts; downgrade not supported (documented) | `api/tests/migrations/` | every PR (required) |
| UI e2e | Playwright against docker-compose stack (Mailpit for email, CDP virtual authenticator for passkeys); axe-core a11y on every page | `ui/e2e/` | every PR (required) |
| Conformance | OpenID Foundation conformance suite (Docker): basic OP, config, PKCE, RP-initiated logout, back-channel logout, DCR, FAPI2 (post-v1) | `.github/workflows/conformance.yml` | **every PR (required)**; full profile matrix nightly |
| Fuzz | cargo-fuzz: authorize params, JWT decode, redirect_uri matcher, PKCE, SCIM filter parser | `api/fuzz/` | **every PR, 60 s per target (required)**; 4 h per target weekly |
| Load | k6 scripts with documented baseline (target 5k token req/s per node, p99 < 50 ms `/token`) | `perf/` | **every PR smoke with thresholds (required)**; full baseline on release |
| Examples | the three example RPs against the docker-compose stack from the built image: `ridm bootstrap --issue-token`, `examples/setup.sh`, then sign-ins in headless Chromium (SSO across apps, permissions allowed and refused, back-channel logout) | `examples/smoke/` | **every PR (required)** |
| Packaging | Helm lint + kind smoke deploy; image boots, migrates, `/readyz`; upgrade from previous tag | `.github/workflows/ci.yml` | **every PR (required)**; upgrade-from-tag on release |
| Coverage | cargo-llvm-cov; floor 80% on `services/`, `oidc/`, `flows/`, `middleware/`; PR fails if below floor | CI | every PR (required) |
| Static | clippy `-D warnings`, `cargo audit`, `cargo deny`, ESLint, `tsc --noEmit` | CI | every PR (required) |

Required status checks on `main` (branch protection): `unit`, `integration`, `isolation`, `security`, `contract`, `migration`, `ui-e2e`, `conformance`, `fuzz-smoke`, `load-smoke`, `packaging`, `examples-smoke`, `coverage`, `static`. Adding a suite means adding it to this list and to branch protection in the same PR.

Conventions
- Integration tests use one shared Postgres/Redis container per test binary; each test creates its own tenant slug so tests run in parallel without interference.
- Every security finding (review, fuzz, conformance, external report) gets a regression test in `api/tests/security/` before the fix is merged.
- Playwright runs against the embedded-UI build so e2e covers the same artifact that ships.
- Mock senders capture messages in memory; tests assert on captured content (links, codes) rather than sleeping or polling.

## 7. Public API surface (v1)

OIDC (per tenant, prefix `/t/{slug}`):
`/.well-known/openid-configuration`, `/.well-known/jwks.json`, `/authorize`, `/par`, `/token`, `/userinfo`, `/introspect`, `/revoke`, `/end_session`, `/device_authorization`, `/register` (DCR) + `/register/{client_id}`, `/broker/{alias}/start|callback`, `/backchannel_logout` (RP side handled by clients).
Global: `/.well-known/webfinger`.

Flows (per tenant): `/flows/{id}`, `/flows/{id}/{step}` (password, passkey, magic-link, email-otp, sms-otp, mfa/{type}, profile, terms, consent, org), `/flows/{id}/cancel`, `/register/*`, `/invitations/{token}`, `/recovery/*`, `/verification/*`, `/device/*`.

Account (per tenant, bearer): `/account/me`, `/account/password`, `/account/email`, `/account/phone`, `/account/credentials/*`, `/account/devices/*`, `/account/sessions/*`, `/account/consents/*`, `/account/identities/*`, `/account/tokens/*` (PATs), `/account/export`, `/account/delete`.

Admin (global, bearer): `/admin/tenants/*` and per tenant: `clients`, `users`, `invitations`, `groups`, `roles`, `resource-servers`, `scopes`, `mappers`, `keys`, `idps`, `sessions`, `audit`, `webhooks`, `ip-rules`, `messaging`, `templates`, `settings`, `export`, `import`, `flags`. SCIM: `/scim/v2/{tenant}/Users|Groups`.

Ops: `/healthz`, `/readyz`, `/metrics`, `/docs` (non-prod).

## 8. Non-goals
Anything cloud-provider-specific in the default build (KMS, managed secrets, proprietary queues). Everything else from the earlier non-goals list has been moved into Phases 12–13 with schema room reserved now (`org_id` columns, `identity_providers.type`, `credentials.type`, `KeyEncryptor` trait).

## 9. Open decisions (confirm before Phase 1)
1. axum (recommended) vs actix-web for consistency with tv/api.
2. Tenant in path (`/t/{slug}`, recommended) vs subdomain-only.
3. Per-tenant signing keys (recommended) vs one global key set.
4. v1 cut line: Phases 0–11 are v1 (large). If you want a faster first release, ship v0.1 after Phase 8 and label 9–11 as v0.2.
