{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ridm.serviceAccountName" . }}
  labels:
    {{- include "ridm.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# rIDM never calls the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
